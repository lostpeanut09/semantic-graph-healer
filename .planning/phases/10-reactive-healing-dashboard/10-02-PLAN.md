---
phase: 10-reactive-healing-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [src/views/dashboard/components/Dashboard.svelte, src/views/dashboard/components/SuggestionCard.svelte, src/views/DashboardView.ts]
autonomous: true
requirements: [UX-01, UI-03]
must_haves:
  truths:
    - "Dashboard shows categorized suggestions in tabs"
    - "DashboardView mounts the Svelte component on open"
    - "DashboardView unmounts the Svelte component on close"
  artifacts:
    - path: "src/views/dashboard/components/Dashboard.svelte"
      provides: "Main tabbed UI"
    - path: "src/views/dashboard/components/SuggestionCard.svelte"
      provides: "Individual suggestion UI"
    - path: "src/views/DashboardView.ts"
      provides: "Obsidian View Wrapper for Svelte"
  key_links:
    - from: "src/views/DashboardView.ts"
      to: "src/views/dashboard/components/Dashboard.svelte"
      via: "svelte mount() function"
---

<objective>
Migrate the manual DOM-based dashboard to Svelte 5 components and implement a tabbed layout for better scannability.

Purpose: Improve scannability and performance using modern UI patterns.
Output: Working Svelte dashboard integrated into Obsidian.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reactive-healing-dashboard/10-01-SUMMARY.md
@src/views/dashboard/DashboardStore.svelte.ts
@src/views/DashboardView.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Implement SuggestionCard component</name>
  <files>src/views/dashboard/components/SuggestionCard.svelte</files>
  <action>
    Create `SuggestionCard.svelte` using Svelte 5 Runes.

    Props (using `$props`):
    - `suggestion`: Suggestion object.
    - `onExecute`: Callback function.
    - `onIgnore`: Callback function.
    - `onReasoning`: Callback function.

    Requirements:
    - Display link title, source path, and confidence (if available).
    - Provide buttons for Execute, Ignore, and Re-reason (if applicable).
    - Use Obsidian CSS variables for styling (`--text-normal`, `--background-secondary`, etc.).
    - Match the visual style of the original `renderSuggestionCard` in `DashboardView.ts`.
  </action>
  <verify>
    <automated>ls src/views/dashboard/components/SuggestionCard.svelte</automated>
  </verify>
  <done>SuggestionCard component ready.</done>
</task>

<task type="auto">
  <name>Implement Main Dashboard component</name>
  <files>src/views/dashboard/components/Dashboard.svelte</files>
  <action>
    Create `Dashboard.svelte` as the main layout.

    Props:
    - `store`: DashboardStore instance.
    - `plugin`: SemanticGraphHealer instance.

    Features:
    - Banner image (assets/banner.png).
    - Header with Title and Settings description.
    - **Tabbed Interface** using a state rune (`let activeTab = $state('all')`):
      - All Issues
      - Structural Gaps
      - Logic Loops
      - Black Holes
      - AI Suggestions
    - Use the store's derived getters to render filtered lists using `{#each ...}`.
    - Render `SuggestionCard` for each item.
    - Render History section at the bottom.
  </action>
  <verify>
    <automated>ls src/views/dashboard/components/Dashboard.svelte</automated>
  </verify>
  <done>Dashboard layout with tabs implemented.</done>
</task>

<task type="auto">
  <name>Update DashboardView to mount Svelte</name>
  <files>src/views/DashboardView.ts</files>
  <action>
    Refactor `src/views/DashboardView.ts`.

    1. Import `mount` and `unmount` from `svelte`.
    2. Import `Dashboard` from `./dashboard/components/Dashboard.svelte`.
    3. Import `DashboardStore` from `./dashboard/DashboardStore.svelte`.
    4. In `onOpen()`:
       - Instantiate `DashboardStore`.
       - Call `mount(Dashboard, { target: this.contentEl, props: { store, plugin: this.plugin } })`.
       - Store the component instance.
    5. In `onClose()`:
       - Call `unmount(componentInstance)`.
    6. Implement `refresh()` to simply call `store.refresh()`.

    Ensure `DASHBOARD_VIEW_TYPE` and other exports remain consistent.
    Delete the old manual DOM rendering code in this file.
  </action>
  <verify>
    <automated>grep "mount(" src/views/DashboardView.ts</automated>
  </verify>
  <done>DashboardView now acts as a thin wrapper for the Svelte app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Svelte -> Obsidian | Svelte components interact with Obsidian API (Notice, Executor). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information Disclosure | UI | mitigate | Ensure no PII or sensitive keys are rendered in the dashboard. |
</threat_model>

<verification>
Check `src/views/DashboardView.ts` for proper `unmount` call in `onClose` to prevent memory leaks. Run `npx vitest tests/views/dashboard/DashboardStore.test.ts` to ensure no regressions in store logic.
</verification>

<success_criteria>
1. Svelte components created.
2. DashboardView successfully mounts the Svelte app.
3. Tab switching works (can be verified visually in Obsidian later).
</success_criteria>

<output>
After completion, create `.planning/phases/10-reactive-healing-dashboard/10-02-SUMMARY.md`
</output>
