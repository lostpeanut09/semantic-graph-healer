---
phase: 10-reactive-healing-dashboard
plan: 04
type: execute
wave: 4
depends_on: ['10-03']
files_modified:
    [
        src/views/dashboard/DashboardStore.svelte.ts,
        src/views/dashboard/components/SuggestionCard.svelte,
        tests/views/dashboard/DashboardStore.test.ts,
    ]
autonomous: true
requirements: [UX-01]
must_haves:
    truths:
        - 'AI reasoning and verification logic is preserved in the store'
        - 'AI verification results are displayed on suggestion cards'
        - 'AI Tribunal features are fully accessible via the new UI'
    artifacts:
        - path: 'src/views/dashboard/DashboardStore.svelte.ts'
          provides: 'AI Tribunal migration logic'
    key_links:
        - from: 'DashboardStore.verifyAI'
          to: 'llm.validateBranching'
          via: 'context-aware validation call'
---

<objective>
Migrate AI verification logic and finalize SuggestionCard UI refinements for AI Tribunal features.

Purpose: Preserve AI Tribunal features in the reactive dashboard and provide a rich UI for AI-assisted healing.
Output: Migrated AI logic, refined UI for AI actions, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reactive-healing-dashboard/10-03-SUMMARY.md
@src/views/dashboard/DashboardStore.svelte.ts
@src/views/dashboard/components/SuggestionCard.svelte
@src/views/DashboardView.ts
</context>

<tasks>

<task type="auto">
  <name>Migrate AI Verification and Reasoning logic</name>
  <files>src/views/dashboard/DashboardStore.svelte.ts</files>
  <action>
    Migrate `handleReasoning`, `handleResolveChoice`, and the Phase 3 AI Verification logic from `DashboardView.ts` to `DashboardStore.svelte.ts`.

    Requirements:
    - `analyze(suggestion)`: Calls `plugin.reasoner.analyze(suggestion)` and updates suggestion state.
    - `verifyAI(suggestion)`:
      - Gathers context via `topology.getContextForAIValidation`.
      - Calls `llm.validateBranching` or `llm.validateTagInheritance`.
      - Manages transient state for the "AI verify" button (e.g., loading status, final result 'Valid'/'Contradict').
    - `resolveChoice(suggestion, winner, losers)`: Calls `plugin.executor.resolveChoice`.

    This ensures complex AI Tribunal logic remains functional and becomes reactive.

  </action>
  <verify>
    <automated>grep "validateBranching" src/views/dashboard/DashboardStore.svelte.ts</automated>
  </verify>
  <done>AI reasoning and verification logic migrated to the reactive store.</done>
</task>

<task type="auto">
  <name>Update SuggestionCard for AI actions</name>
  <files>src/views/dashboard/components/SuggestionCard.svelte</files>
  <action>
    Update `SuggestionCard.svelte` to support AI-specific interactions.

    Requirements:
    - Wire "AI verify" and "Re-reason" buttons to the new `DashboardStore` methods.
    - Show the AI reasoning log link if reasoning is available.
    - Display AI verification results (e.g., green/red badge) when available.
    - Handle loading states for AI operations using store reactivity.

  </action>
  <verify>
    <automated>grep "verifyAI" src/views/dashboard/components/SuggestionCard.svelte</automated>
  </verify>
  <done>SuggestionCard updated with AI action wiring and refined visual feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Verify AI logic in store tests</name>
  <files>tests/views/dashboard/DashboardStore.test.ts</files>
  <behavior>
    - Mock `llm` and `topology` for AI verification.
    - Verify `verifyAI` correctly calls LLM with gathered context.
    - Verify `analyze` calls reasoner and updates state.
    - Verify `resolveChoice` calls executor correctly.
  </behavior>
  <action>
    Extend Vitest unit tests in `DashboardStore.test.ts` to cover the migrated AI Tribunal logic.
  </action>
  <verify>
    <automated>npx vitest tests/views/dashboard/DashboardStore.test.ts --run</automated>
  </verify>
  <done>Migrated AI logic verified by automated tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                   |
| ----------------- | ------------------------------------------------------------- |
| Plugin -> LLM API | Context data is sent to external LLM provider for validation. |

## STRIDE Threat Register

| Threat ID | Category               | Component   | Disposition | Mitigation Plan                                                          |
| --------- | ---------------------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| T-10-04   | Information Disclosure | LLM context | mitigate    | Ensure context gathered by topology service is sanitized before sending. |

</threat_model>

<verification>
Ensure AI verification logic handles network errors gracefully without crashing the UI. Run `npx vitest tests/views/dashboard/DashboardStore.test.ts` for full verification of AI features.
</verification>

<success_criteria>

1. AI verification and reasoning are fully functional in the new UI.
2. AI-specific actions (Verify, Re-reason) work correctly.
3. AI results are clearly displayed in the UI.
4. All AI-related store tests pass.
   </success_criteria>

<output>
After completion, create `.planning/phases/10-reactive-healing-dashboard/10-04-SUMMARY.md`
</output>
