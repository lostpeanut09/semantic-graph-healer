---
phase: "06-advanced-topological-metrics"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/types.ts",
  "src/core/CacheService.ts",
  "src/core/GraphEngine.ts",
  "src/core/LinkPredictionEngine.ts"
]
autonomous: true
requirements: [TOPOL-02, TOPOL-03]

must_haves:
  truths:
    - "Topological scores (PageRank, Betweenness, Louvain) are persisted in the cache."
    - "Link prediction logic is encapsulated in a dedicated engine with weighted scoring."
    - "GraphEngine uses cached metrics to avoid redundant heavy computations."
  artifacts:
    - path: "src/core/LinkPredictionEngine.ts"
      provides: "Formalized link prediction service"
    - path: "src/core/CacheService.ts"
      provides: "Persistence for graph metrics"
  key_links:
    - from: "src/core/GraphEngine.ts"
      to: "src/core/CacheService.ts"
      via: "loading/saving topologicalScores"
    - from: "src/core/GraphEngine.ts"
      to: "src/core/LinkPredictionEngine.ts"
      via: "similarity analysis orchestration"
---

<objective>
Formalize the Link Prediction Engine and implement persistence for topological scores in the CacheService.

Purpose: Improve performance by avoiding redundant analysis and provide a robust foundation for advanced topological suggestions.
Output: LinkPredictionEngine class and updated CacheService with metric persistence.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-advanced-topological-metrics/06-RESEARCH.md
@src/core/workers/graph-analysis-core.ts
@src/core/GraphEngine.ts
@src/core/CacheService.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Update types and CacheService for persistence</name>
  <files>src/types.ts, src/core/CacheService.ts</files>
  <action>
    - Add `topologicalScores` to the `HealerCache` interface in `CacheService.ts`.
    - Interface should include: `pageRank`, `betweenness`, `communities` (all `Record<string, number>`), `lastAnalysisTimestamp`, and `graphVersion`.
    - Ensure `topologicalScores` is initialized in `DEFAULT_CACHE`.
    - Update `src/types.ts` if any new types for these metrics are needed (e.g., `TopologicalMetrics`).
  </action>
  <verify>
    <automated>npm test tests/core/services/CacheService.test.ts</automated>
  </verify>
  <done>CacheService supports persisting topological metrics.</done>
</task>

<task type="auto">
  <name>Task 1: Formalize LinkPredictionEngine</name>
  <files>src/core/LinkPredictionEngine.ts</files>
  <action>
    - Create `src/core/LinkPredictionEngine.ts` following the research pattern.
    - Encapsulate the similarity analysis logic currently in `GraphEngine.runSimilarityAnalysis`.
    - Implement a method `predictLinks(graph: DirectedGraph, options: PredictionOptions)` that uses the worker for heavy lifting but handles score normalization and suggestion synthesis.
    - Use `linkPredictionWeights` from settings (Jaccard, Adamic-Adar, RA).
    - Ensure it handles the temporal decay factor based on `fileStats`.
  </action>
  <verify>
    <automated>npm test tests/core/LinkPredictionEngine.test.ts</automated>
  </verify>
  <done>LinkPredictionEngine is a dedicated service with weighted prediction logic.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Persistence and Engine into GraphEngine</name>
  <files>src/core/GraphEngine.ts</files>
  <action>
    - Refactor `GraphEngine.ts` to use `LinkPredictionEngine` for similarity analysis.
    - Update `runPageRankAnalysis`, `runCommunityDetection`, and `runBetweennessAnalysis` to check the cache first.
    - If `topologicalScores` exist in cache, and `graphVersion` matches current graph, and they are less than 1 hour old, return cached suggestions instead of running analysis.
    - After running worker analysis, save results back to `CacheService.topologicalScores`.
    - Implement `clearTopologicalCache()` to force re-analysis.
  </action>
  <verify>
    <automated>npm test tests/core/GraphEngine.test.ts</automated>
  </verify>
  <done>GraphEngine efficiently manages analysis with persistence and dedicated engine delegation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker â†’ main thread | analysis results cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | GraphWorkerService | mitigate | Implementation of workerTimeout (120s) and maxNodes/maxEdges guardrails. |
| T-06-02 | Information Disclosure | CacheService | accept | Cache is stored locally in the vault; standard filesystem permissions apply. |
</threat_model>

<verification>
- Run full test suite: `npm test`
- Verify `healer-cache.json` contains `topologicalScores` after running analysis.
- Verify `LinkPredictionEngine` correctly applies weights in unit tests.
</verification>

<success_criteria>
- Topological metrics are stored in `healer-cache.json`.
- Link prediction suggestions are generated using the new engine.
- Re-running analysis within 1 hour uses cached scores if the graph hasn't changed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-topological-metrics/06-01-SUMMARY.md`
</output>
