---
phase: '06-advanced-topological-metrics'
plan: '02'
type: execute
wave: 2
depends_on: ['06-01']
files_modified: ['src/views/sections/DeepAnalyticsSettings.ts', 'src/core/GraphEngine.ts']
autonomous: true
requirements: [TOPOL-02, TOPOL-03]

must_haves:
    truths:
        - 'Users can tune link prediction weights (Jaccard, AA, RA) in settings.'
        - 'Users can configure MOC saturation and Black Hole thresholds.'
        - 'The plugin suggests new MOCs for large Louvain clusters using PageRank authorities.'
    artifacts:
        - path: 'src/views/sections/DeepAnalyticsSettings.ts'
          provides: 'Expanded analytics UI'
    key_links:
        - from: 'src/views/sections/DeepAnalyticsSettings.ts'
          to: 'src/core/GraphEngine.ts'
          via: 'setting updates and cache clearing'
---

<objective>
Expand the Deep Analytics settings UI and refine MOC (Map of Content) suggestions based on community detection and centrality metrics.

Purpose: Provide user control over analytical parameters and improve the utility of structural suggestions.
Output: Expanded settings UI and enhanced MOC suggestion logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-advanced-topological-metrics/06-RESEARCH.md
@.planning/phases/06-advanced-topological-metrics/06-01-SUMMARY.md
@src/views/sections/DeepAnalyticsSettings.ts
@src/core/GraphEngine.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand DeepAnalyticsSettings UI</name>
  <files>src/views/sections/DeepAnalyticsSettings.ts</files>
  <action>
    - Add setting sliders/inputs for `linkPredictionWeights` (Jaccard, Adamic-Adar, Resource Allocation).
    - Add numeric input for `mocSaturationThreshold` (used in cluster suggestions).
    - Add numeric input for `blackHoleThreshold` (used in diagnostic analysis).
    - Add numeric input for `bridgeScrutinyMaxDepth`.
    - Add a "Clear Analytical Cache" button that calls `engine.clearTopologicalCache()` and shows a Notice.
    - Ensure all changes call `plugin.saveSettings()`.
  </action>
  <verify>
    <checkpoint:human-verify gate="blocking">
      <what-built>Expanded Deep Analytics Settings UI.</what-built>
      <how-to-verify>
        1. Open Settings -> Semantic Graph Healer -> Deep Analytics.
        2. Verify new controls for weights and thresholds are visible and functional.
        3. Change a weight and verify it persists after closing/opening settings.
      </how-to-verify>
      <resume-signal>Settings UI verified</resume-signal>
    </checkpoint:human-verify>
  </verify>
  <done>Settings UI allows full control over topological analysis parameters.</done>
</task>

<task type="auto">
  <name>Task 2: Refine MOC (Thematic Hub) Suggestions</name>
  <files>src/core/GraphEngine.ts</files>
  <action>
    - Enhance `runCommunityDetection` to produce "Thematic Hub" suggestions.
    - For each significant Louvain cluster (size > 5):
      - Identify the top 3 PageRank notes in that cluster.
      - If no existing MOC note covers this cluster, suggest creating a "Thematic Hub" note.
      - Use `mocSaturationThreshold` to determine if a cluster is "saturated" enough to warrant an MOC.
      - Suggest a name for the hub based on the highest PageRank note (e.g., "MOC: [Note Name] Cluster").
  </action>
  <verify>
    <automated>npm test tests/core/GraphEngine.moc.test.ts</automated>
  </verify>
  <done>MOC suggestions are intelligently derived from community clusters and centrality pillars.</done>
</task>

<task type="auto">
  <name>Task 3: Final Verification and UAT</name>
  <files>src/core/GraphEngine.ts</files>
  <action>
    - Ensure `runSimilarityAnalysis` uses the updated settings weights.
    - Run a full graph analysis and verify that multiple types of topological suggestions are generated (PageRank info, Link Predictions, MOC suggestions).
    - Verify that clearing the cache works as expected.
  </action>
  <verify>
    <checkpoint:human-verify gate="blocking">
      <what-built>Full Phase 6 topological analysis pipeline.</what-built>
      <how-to-verify>
        1. Click "Run Full Analysis" in the Dashboard (or trigger via command).
        2. Check the suggestions for "Predicted Link" and "Thematic Hub".
        3. Verify the "Source" descriptions correctly explain the metric used (e.g., "Predicted via Jaccard").
      </how-to-verify>
      <resume-signal>Analytical pipeline verified</resume-signal>
    </checkpoint:human-verify>
  </verify>
  <done>Phase 6 implementation is verified and stable.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                       |
| ----------------------------- | --------------------------------- |
| User Settings â†’ GraphEngine | Thresholds and weights cross here |

## STRIDE Threat Register

| Threat ID | Category  | Component   | Disposition | Mitigation Plan                                                  |
| --------- | --------- | ----------- | ----------- | ---------------------------------------------------------------- |
| T-06-03   | Tampering | Settings UI | mitigate    | Input validation on thresholds (e.g., min=0, max=1 for weights). |

</threat_model>

<verification>
- Verify that MOC suggestions are only generated for clusters exceeding the saturation threshold.
- Verify that link prediction scores change when weights are modified in settings.
</verification>

<success_criteria>

- User can tune analytical parameters in the UI.
- Thematic Hub (MOC) suggestions are generated for relevant clusters.
- All Phase 6 goals are achieved.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-topological-metrics/06-02-SUMMARY.md`
</output>
