---
phase: 05-topological-diagnostics-gaps-loops
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/core/services/GraphWorkerService.ts, src/core/GraphEngine.ts]
autonomous: true
requirements: [TOPOL-01, TOPOL-04, TOPOL-05]
must_haves:
  truths:
    - "GraphEngine serializes edge types (up, down, next, prev, same, related) to the worker"
    - "GraphWorkerService successfully dispatches TOPOLOGY_DIAGNOSTICS requests"
  artifacts:
    - path: "src/core/GraphEngine.ts"
      provides: "Unified runTopologicalAnalysis method and typed graph serialization"
  key_links:
    - from: "GraphEngine"
      to: "GraphWorkerService"
      via: "runAnalysis call with TOPOLOGY_DIAGNOSTICS type"
---

<objective>
Update the service layer to support the new topological diagnostics. This involves extending the graph serialization in `GraphEngine` to include edge types and adding the dispatch logic in `GraphWorkerService`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/core/GraphEngine.ts
@src/core/services/GraphWorkerService.ts
@.planning/phases/05-topological-diagnostics-gaps-loops/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GraphWorkerService types</name>
  <files>src/core/services/GraphWorkerService.ts</files>
  <action>Add `TOPOLOGY_DIAGNOSTICS` to the `AnalysisType` type definition in `GraphWorkerService.ts`.</action>
  <verify>
    <automated>grep "TOPOLOGY_DIAGNOSTICS" src/core/services/GraphWorkerService.ts</automated>
  </verify>
  <done>AnalysisType includes the new diagnostic type.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed graph serialization in GraphEngine</name>
  <files>src/core/GraphEngine.ts</files>
  <action>
    1. Update `GraphEngine.buildGraph` to identify edge types based on `settings.hierarchies`.
    2. When adding an edge, store its relationship type (e.g., 'up', 'down', 'next', 'prev') in the edge attributes.
    3. Ensure `getSerializedEdges()` includes the `type` attribute for each edge.
    4. Handle Pitfall 1 from RESEARCH.md: Ensure edge types are preserved during serialization to the worker.
  </action>
  <verify>
    <automated>grep "type:" src/core/GraphEngine.ts</automated>
  </verify>
  <done>GraphEngine serializes typed edges compatible with topological diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Add runTopologicalAnalysis to GraphEngine</name>
  <files>src/core/GraphEngine.ts</files>
  <action>
    Implement `runTopologicalAnalysis(options)`:
    - Serialize current graph (nodes + typed edges).
    - Call `this.context.graphWorkerService.runAnalysis('TOPOLOGY_DIAGNOSTICS', nodes, edges, options)`.
    - Return the raw results (bridges, cycles, blackHoles).
  </action>
  <verify>
    <automated>grep "runTopologicalAnalysis" src/core/GraphEngine.ts</automated>
  </verify>
  <done>GraphEngine exposes worker-backed topological analysis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GraphEngine → Worker | Serialization of hierarchy types |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | Serialization | accept | Edge types are internal metadata, no PII. |
</threat_model>

<success_criteria>
`GraphEngine` can successfully build a typed graph and dispatch a topological analysis request to the worker, receiving results for bridges, cycles, and sinks.
</success_criteria>

<output>
After completion, create `.planning/phases/05-topological-diagnostics-gaps-loops/05-02-SUMMARY.md`
</output>
