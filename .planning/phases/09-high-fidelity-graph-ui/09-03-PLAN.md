---
phase: 09-high-fidelity-graph-ui
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified: [src/views/components/GraphPopup.ts, src/views/GraphVisualizerView.ts]
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Clicking a problematic node or link triggers a popup"
    - "The popup displays reasoning and an 'Execute Fix' button"
    - "Executing a fix from the popup updates the graph and triggers an Obsidian Notice"
  artifacts:
    - path: "src/views/components/GraphPopup.ts"
      provides: "In-graph popup component for fixes"
---

<objective>
Implement the interactive popup for graph elements, displaying AI reasoning and allowing direct execution of healing actions via SuggestionExecutor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-high-fidelity-graph-ui/09-02-PLAN.md
@src/core/SuggestionExecutor.ts
@src/views/DashboardView.ts
</context>

<tasks>

<task type="auto">
  <name>Implement GraphPopup component</name>
  <files>src/views/components/GraphPopup.ts</files>
  <action>
    Create `src/views/components/GraphPopup.ts`.
    The popup should be an overlay (absolute positioned) within the graph container.
    Content must include:
    - Title (Node/Link name)
    - Reasoning summary (from `suggestion.reasoning` or `suggestion.source`)
    - "Execute Fix" button (visible only if a fixable suggestion exists for this element)
    - "Dismiss" button
  </action>
  <verify>
    <automated>grep -q "Execute Fix" src/views/components/GraphPopup.ts</automated>
  </verify>
  <done>GraphPopup component implemented.</done>
</task>

<task type="auto">
  <name>Integrate Popup with Graph View</name>
  <files>src/views/GraphVisualizerView.ts</files>
  <action>
    Wire `onNodeClick` and `onLinkClick` in `GraphVisualizerView` to show the `GraphPopup`.
    Pass the relevant `Suggestion` object (matched by ID or metadata) to the popup.
    Implement the "Execute Fix" logic by calling `plugin.executor.execute()` or `executeRelink()`.
    Ensure the graph refreshes after a successful fix.
  </action>
  <verify>
    <automated>grep -q "onNodeClick" src/views/GraphVisualizerView.ts</automated>
  </verify>
  <done>Popup integrated with graph interactions and suggestion execution.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Verify Graph Interaction</name>
  <what-built>3D Graph view with interactive popups and fix execution.</what-built>
  <how-to-verify>
    1. Open Obsidian.
    2. Run "Open Healer Graph" command.
    3. Find a pulsating node (Cycle) or a dotted link (Gap).
    4. Click it to open the popup.
    5. Verify reasoning text is displayed.
    6. Click "Execute Fix".
    7. Verify a Notice appears and the graph updates (element should be fixed/removed if it was an error).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> SuggestionExecutor | File system changes are triggered by user action in the UI. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Elevation of Privilege | GraphPopup | mitigate | Suggestion execution is limited to pre-analyzed healing actions via the hardened `SuggestionExecutor`. |
</threat_model>

<success_criteria>
1. Clicking elements shows the correct popup.
2. AI reasoning is visible in the popup.
3. Fixes can be executed directly from the graph view.
</success_criteria>

<output>
After completion, create `.planning/phases/09-high-fidelity-graph-ui/09-03-SUMMARY.md`
</output>
