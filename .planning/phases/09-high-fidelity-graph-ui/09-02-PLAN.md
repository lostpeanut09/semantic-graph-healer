---
phase: 09-high-fidelity-graph-ui
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [src/views/GraphVisualizerView.ts, src/main.ts, styles.css]
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "A 3D Graph view can be opened from the Obsidian workspace"
    - "Hierarchical cycles pulsate in red"
    - "Structural gaps appear as dotted ghost edges"
  artifacts:
    - path: "src/views/GraphVisualizerView.ts"
      provides: "Obsidian ItemView for the 3D Graph"
  key_links:
    - from: "src/views/GraphVisualizerView.ts"
      to: "src/core/utils/GraphMapper.ts"
      via: "Rendering data transformation"
      pattern: "mapper.mapToForceGraph(data)"
---

<objective>
Implement the core 3D Graph view using 3d-force-graph, applying the visual semantics for topological errors (cycles and gaps).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-high-fidelity-graph-ui/09-01-PLAN.md
@.planning/phases/09-high-fidelity-graph-ui/09-RESEARCH.md
@src/views/DashboardView.ts
@src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Implement GraphVisualizerView</name>
  <files>src/views/GraphVisualizerView.ts</files>
  <action>
    Create `src/views/GraphVisualizerView.ts` extending `ItemView`.
    Register the view type `healer-graph-view`.
    Initialize `3d-force-graph` in `onOpen`.
    Implement pulsating cycles using the `nodeColor` reactive pattern from RESEARCH.md.
    Implement dotted ghost edges using `linkDash` (with `linkWidth(0)` as required).
    Ensure `onClose` calls `this.graph._destructor()` and cleans up the scene to prevent memory leaks.
  </action>
  <verify>
    <automated>grep -q "_destructor" src/views/GraphVisualizerView.ts</automated>
  </verify>
  <done>GraphVisualizerView implemented with visual indicators for errors.</done>
</task>

<task type="auto">
  <name>Register Graph View and Add Command</name>
  <files>src/main.ts</files>
  <action>
    Register `GraphVisualizerView` in the main plugin file.
    Add a command "Open Healer Graph" to open the view in a new leaf.
    Ensure the view is correctly destroyed on plugin unload.
  </action>
  <verify>
    <automated>grep -q "healer-graph-view" src/main.ts</automated>
  </verify>
  <done>Graph view registered and accessible via command.</done>
</task>

<task type="auto">
  <name>Add Graph Styles</name>
  <files>styles.css</files>
  <action>
    Add CSS classes for the graph container and any UI overlays.
    Ensure the container takes up full width/height of the leaf.
  </action>
  <verify>
    <automated>grep -q "healer-graph-container" styles.css</automated>
  </verify>
  <done>Styling applied for the graph view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Obsidian UI -> GraphVisualizerView | View lifecycle is managed by the host application. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Denial of Service | GraphVisualizerView | mitigate | Implement `_destructor` cleanup in `onClose` to prevent memory leaks during repeated view opening. |
</threat_model>

<success_criteria>
1. The "Open Healer Graph" command opens the custom 3D view.
2. Cycle nodes pulsate red as specified.
3. Bridge gaps appear as dotted links.
4. No significant memory increase after closing the view (verified via manual check/profiler).
</success_criteria>

<output>
After completion, create `.planning/phases/09-high-fidelity-graph-ui/09-02-SUMMARY.md`
</output>
