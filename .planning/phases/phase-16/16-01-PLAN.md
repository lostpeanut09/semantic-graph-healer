---
phase: 16-wasm-graph-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, .config/esbuild.config.mjs, src/core/workers/ladybug-worker.ts, src/core/services/LadybugService.ts, src/core/adapters/LadybugAdapter.ts, tests/benchmarks/LadybugBenchmark.test.ts, src/core/adapters/LadybugAdapter.test.ts, src/core/services/LadybugService.test.ts]
autonomous: false
requirements: [INFRA-06, INFRA-07, HARDEN-10, PERF-01]

must_haves:
  truths:
    - "LadybugDB initializes in a background worker without blocking the main UI thread."
    - "Graph schema (Node/SemanticLink) is successfully applied to LadybugDB."
    - "Topological diagnostics (Bridges, Cycles, Black Holes) return correct results via Cypher queries."
    - "Benchmarks on 50k and 100k nodes demonstrate >10x speedup over previous implementation within 256MB RAM."
  artifacts:
    - path: "src/core/workers/ladybug-worker.ts"
      provides: "LadybugDB WASM worker entry point"
      min_lines: 50
    - path: "src/core/services/LadybugService.ts"
      provides: "Service for managing LadybugDB lifecycle and queries"
      exports: ["LadybugService"]
    - path: "src/core/adapters/LadybugAdapter.ts"
      provides: "Data sync bridge from IMetadataAdapter to LadybugDB"
      exports: ["LadybugAdapter"]
  key_links:
    - from: "src/core/adapters/LadybugAdapter.ts"
      to: "LadybugDB"
      via: "Batched Cypher INSERT/COPY"
    - from: "src/core/services/LadybugService.ts"
      to: "LadybugWorker"
      via: "Web Worker message bridge"
---

<objective>
Implement the LadybugDB WASM graph engine to support large-scale vaults (50k+ nodes). This involves setting up the infrastructure, defining the graph schema, implementing a synchronization layer from existing metadata adapters, and porting topological diagnostics to Cypher.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-16/RESEARCH.md
@.planning/phases/phase-16/16-CONTEXT.md
@.planning/phases/phase-16/16-VALIDATION.md
@src/core/workers/graph-analysis-core.ts
@src/core/GraphEngine.ts
@src/core/adapters/IMetadataAdapter.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package Legitimacy Gate (icebug)</name>
  <what-built>Preparation for installing new dependencies.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/icebug
    2. Confirm it is the official High-performance NetworKit fork for LadybugDB as described in RESEARCH.md.
    3. If legitimate, approve to proceed with installation.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Ladybug Infrastructure & Worker Bridge</name>
  <files>package.json, .config/esbuild.config.mjs, src/core/workers/ladybug-worker.ts, src/core/services/LadybugService.ts, src/core/services/LadybugService.test.ts</files>
  <behavior>
    - Test 1: LadybugService initializes the worker and queues messages before worker is ready.
    - Test 2: LadybugService falls back to legacy engine if WASM initialization fails (e.g., SharedArrayBuffer missing).
    - Test 3: LadybugWorker successfully loads the 12MB binary lazily on first query.
  </behavior>
  <action>
    1. Install `@ladybugdb/wasm-core` (v0.16.1) and `icebug`.
    2. Add `ladybug-worker: 'src/core/workers/ladybug-worker.ts'` as a new entry point in `.config/esbuild.config.mjs` (target browser, format iife).
    3. Create `src/core/workers/ladybug-worker.ts` to initialize LadybugDB. Use `IDBFS` for persistence to IndexedDB.
    4. Create `src/core/services/LadybugService.ts`. It must manage the worker lifecycle, handle message routing, and implement the fallback logic.
    5. Implement the "Lazy Loading" pattern: the worker should only fetch and compile the WASM binary when the first analysis request is received, not on plugin startup.
  </action>
  <verify>
    <automated>npm run build && npm test src/core/services/LadybugService.test.ts</automated>
  </verify>
  <done>LadybugDB worker is bundled, the service can initialize it successfully, and fallback mechanism is tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema Definition & Data Sync</name>
  <files>src/core/workers/ladybug-worker.ts, src/core/adapters/LadybugAdapter.ts, src/core/adapters/LadybugAdapter.test.ts</files>
  <behavior>
    - Test 1: LadybugAdapter correctly transforms SemanticLinkEdge arrays into Cypher INSERT/COPY statements.
    - Test 2: Node and Relation tables are created with correct schemas on first sync.
    - Test 3: Syncing 1000 nodes completes without errors and data is queryable via Cypher.
  </behavior>
  <action>
    1. Define Node table in the worker: `CREATE NODE TABLE Node(path STRING, label STRING, size INT64, PRIMARY KEY (path))`.
    2. Define Relation table: `CREATE REL TABLE SemanticLink(FROM Node TO Node, weight DOUBLE, type STRING)`.
    3. Implement `LadybugAdapter.ts`: it should subscribe to `IMetadataAdapter` events and sync changes to LadybugDB.
    4. Optimize ingestion using batched statements or the `COPY` command if supported by the WASM core.
  </action>
  <verify>
    <automated>npm test src/core/adapters/LadybugAdapter.test.ts</automated>
  </verify>
  <done>Graph schema is applied and vault data is synchronized into LadybugDB.</done>
</task>

<task type="auto">
  <name>Task 3: Cypher Algos & 100k Benchmark</name>
  <files>src/core/workers/ladybug-worker.ts, tests/benchmarks/LadybugBenchmark.test.ts</files>
  <action>
    1. Port topological diagnostics to Cypher:
       - **Black Holes**: `MATCH (n) WHERE outdegree(n) = 0 AND indegree(n) > $threshold RETURN n.path`
       - **Bridges**: `MATCH (a)-[r1]->(b)-[r2]->(c) WHERE r1.type = r2.type AND NOT (a)-[]->(c) AND a <> c RETURN b.path`
       - **Cycles**: Use LadybugDB's `RECURSIVE` path search or built-in cycle detection.
    2. Integrate `icebug` algorithms for PageRank and Louvain community detection.
    3. Implement `tests/benchmarks/LadybugBenchmark.test.ts`:
       - Generate synthetic graphs of 50,000 and 100,000 nodes.
       - Verify Bridge detection and Louvain clustering meet performance targets (>10x speedup vs legacy).
       - Ensure memory usage remains within the 256MB boundary for 50k nodes.
  </action>
  <verify>
    <automated>npx vitest tests/benchmarks/LadybugBenchmark.test.ts</automated>
  </verify>
  <done>Cypher diagnostics are fully functional and performance targets for large-scale vaults are verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main Thread -> Worker | Structured cloning of link data |
| Worker -> LadybugDB | WASM memory boundary (isolated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | DoS | LadybugDB Worker | mitigate | Implement execution timeouts for Cypher queries (default 60s) to prevent UI lock if a complex query hangs the worker. |
| T-16-02 | Information Disclosure | IndexedDB | mitigate | WASM persistence uses standard browser isolation; no cross-vault or cross-plugin data leakage is possible. |
| T-16-SC | Tampering | npm installs | mitigate | Verified packages in RESEARCH.md; blocking human checkpoint for [ASSUMED] package `icebug`. |
</threat_model>

<verification>
1. `npm run build` confirms `ladybug-worker.js` is correctly bundled.
2. `npm test src/core/adapters/LadybugAdapter.test.ts` passes with high-fidelity sync.
3. `npx vitest tests/benchmarks/LadybugBenchmark.test.ts` confirms >10x speedup on 50k nodes.
</verification>

<success_criteria>
- LadybugDB initializes successfully in background worker.
- Cypher queries return identical results to legacy Graphology implementation.
- PageRank and Louvain benchmarks show >10x speedup on 50k+ nodes.
- Memory usage stays within 256MB for 50k node vault.
</success_criteria>

<output>
Create `.planning/phases/phase-16/16-01-SUMMARY.md` when done
</output>
