---
phase: 07-ai-tribunal-similarity-analysis
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/views/sections/TribunalSettings.ts, src/views/SettingsTab.ts, src/views/DashboardView.ts]
autonomous: true
requirements: [AI-01, AI-03]
must_haves:
  truths:
    - "User can configure the AI Tribunal triage threshold in settings."
    - "Tribunal verification status is visible on suggestion cards in the dashboard."
    - "Stable and Conflict states are clearly differentiated in the UI."
  artifacts:
    - path: "src/views/sections/TribunalSettings.ts"
      provides: "Threshold configuration UI"
    - path: "src/views/DashboardView.ts"
      provides: "Consensus status visualization"
---

<objective>
Expose the AI Tribunal controls to the user and visualize the verification consensus on suggestions. This provides transparency into the dual-model reasoning process and allows users to trust "Verified" suggestions while scrutinizing "Conflicts".

Purpose: User control and transparency for AI verification.
Output: Enhanced settings and dashboard UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/views/sections/TribunalSettings.ts
@src/views/SettingsTab.ts
@src/views/DashboardView.ts
</context>

<tasks>

<task type="auto">
  <name>Hardening Tribunal Settings UI</name>
  <files>src/views/sections/TribunalSettings.ts</files>
  <action>
    Update src/views/sections/TribunalSettings.ts:
    - Add a Slider setting for "Triage Threshold" (aiConfidenceThreshold).
    - Add descriptive text explaining that the Tribunal only triggers if Primary confidence is below this threshold.
    - Improve the "Identical Model" warning to be more prominent.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Tribunal settings include triage threshold control.</done>
</task>

<task type="auto">
  <name>Consensus Visualization in Dashboard</name>
  <files>src/views/DashboardView.ts</files>
  <action>
    Update renderSuggestionCard in src/views/DashboardView.ts:
    - Check suggestion.reasoning.tribunalStatus.
    - If STABLE: Render a "Verified" badge (e.g., green checkmark icon).
    - If CONFLICT: Render a "Consensus Conflict" warning badge (e.g., red alert icon) and potentially highlight the secondary model's alternative in the card or reasoning view.
    - If UNCERTAIN: Render a "Verification Pending/Incomplete" indicator.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Tribunal status is visually represented on all AI-processed suggestions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> User | Visual representation of AI reasoning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | DashboardView | mitigate | Ensure badges are derived strictly from cached suggestion data and not recalculated on the fly to avoid UI flickering/inconsistency. |
</threat_model>

<verification>
Open the Healer Dashboard and verify that suggestions that went through the Tribunal show the appropriate verification badge.
</verification>

<success_criteria>
Users can easily identify which suggestions have been cross-verified by the AI Tribunal.
</success_criteria>

<output>
After completion, create .planning/phases/07-ai-tribunal-similarity-analysis/07-03-SUMMARY.md
</output>
