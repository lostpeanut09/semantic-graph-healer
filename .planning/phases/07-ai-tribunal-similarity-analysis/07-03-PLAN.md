---
phase: 07-ai-tribunal-similarity-analysis
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [src/views/DashboardView.ts]
autonomous: true
requirements: [AI-01]
must_haves:
  truths:
    - "Dashboard UI explicitly displays the final Tribunal Verdict (STABLE or CONFLICT)."
    - "Dashboard UI displays the primary reasoning."
    - "Dashboard UI conditionally displays the secondary reasoning (if the secondary model was engaged)."
  artifacts:
    - path: "src/views/DashboardView.ts"
      provides: "Audit Transparency UI for Tribunal Consensus"
  key_links:
    - from: "src/views/DashboardView.ts"
      to: "ReasoningResult"
      via: "Rendering suggestion details"
      pattern: "suggestion.reasoning.secondaryReasoning"
---

<objective>
Implement Tribunal UX & Audit Transparency in the Healing Dashboard.

Purpose: To provide the user with full transparency into the AI's decision-making process by showing both models' reasoning and the final verdict.
Output: Updated DashboardView.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ai-tribunal-similarity-analysis/07-CONTEXT.md
@.planning/phases/07-ai-tribunal-similarity-analysis/07-RESEARCH.md
<interfaces>
From src/types.ts:
```typescript
export interface ReasoningResult {
  verdict: 'STABLE' | 'CONFLICT';
  confidenceScore: number;
  primaryReasoning: string;
  secondaryReasoning?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dashboard Suggestion UI</name>
  <files>src/views/DashboardView.ts</files>
  <action>Update the rendering logic for suggestions in the Healing Dashboard to reflect the properties of `ReasoningResult`. Add an indicator for the `verdict` (e.g., green for STABLE, yellow/red for CONFLICT). Display the `primaryReasoning`. If `secondaryReasoning` is present, render it within a collapsible section or clearly distinct area to show the audit trail without taking up excessive vertical space.</action>
  <verify>
    <automated>grep -c "secondaryReasoning" src/views/DashboardView.ts</automated>
  </verify>
  <done>The Dashboard properly surfaces the Tribunal Verdict and audit trail (both reasonings) for suggestions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin Data -> Dashboard UI | Rendering LLM text outputs to the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Spoofing/Tampering | DashboardView | mitigate | Ensure LLM-generated reasonings are properly sanitized before rendering (e.g., using Obsidian's safe DOM insertion methods or sanitizing HTML) to prevent XSS. |
</threat_model>

<verification>
Ensure the project builds correctly (`npm run build`).
</verification>

<success_criteria>
Dashboard displays complete Tribunal insights with clear verdict UI and collapsible audit trails.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-tribunal-similarity-analysis/07-03-SUMMARY.md`
</output>