---
phase: 07-ai-tribunal-similarity-analysis
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/core/adapters/SmartConnectionsAdapter.ts, src/core/LinkPredictionEngine.ts, tests/core/LinkPredictionEngine.test.ts]
autonomous: true
requirements: [AI-03]
must_haves:
  truths:
    - "LinkPredictionEngine incorporates Smart Connections vector similarity into its ranking logic."
    - "Suggestions are scored using the Harmonized Topological Ranking (HTR) formula."
    - "SmartConnectionsAdapter provides normalized similarity scores (0.0 - 1.0)."
  artifacts:
    - path: "src/core/LinkPredictionEngine.ts"
      provides: "HTR hybrid scoring logic"
  key_links:
    - from: "src/core/LinkPredictionEngine.ts"
      to: "src/core/adapters/SmartConnectionsAdapter.ts"
      via: "getRelatedNotes call"
---

<objective>
Integrate Smart Connections vector similarity into the LinkPredictionEngine to enable "Harmonized Topological Ranking" (HTR). This identifies related notes that are semantically close even if they lack direct topological links.

Purpose: Improve link prediction accuracy by combining structural and semantic signals.
Output: Enhanced LinkPredictionEngine with HTR support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/adapters/SmartConnectionsAdapter.ts
@src/core/LinkPredictionEngine.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>SmartConnections Score Normalization</name>
  <files>src/core/adapters/SmartConnectionsAdapter.ts</files>
  <behavior>
    - getRelatedNotes must return scores normalized between 0.0 and 1.0.
    - If Smart Connections returns scores in a different range (e.g. 0-100 or -1 to 1), they must be remapped.
    - Return a score of 0.5 for AJSON fallback matches (neutral).
  </behavior>
  <action>
    Review and update getRelatedNotes in SmartConnectionsAdapter.ts:
    - Ensure scores from SC API are normalized.
    - Validate that fallback results correctly return 0.5.
  </action>
  <verify>
    <automated>npx vitest run tests/core/adapters/SmartConnectionsAdapter.test.ts</automated>
  </verify>
  <done>SmartConnectionsAdapter provides normalized similarity scores.</done>
</task>

<task type="auto" tdd="true">
  <name>Implement HTR in LinkPredictionEngine</name>
  <files>src/core/LinkPredictionEngine.ts</files>
  <behavior>
    - For each topological candidate:
      - Fetch vector similarity from SmartConnectionsAdapter.
      - Apply formula: HTR_Score = (VectorSim * 0.6) + (TopolSim * 0.4).
    - If Smart Connections is disabled or unavailable, fallback to 100% Topological weight.
  </behavior>
  <action>
    Update LinkPredictionEngine.predictLinks:
    - Integrate SmartConnectionsAdapter.
    - For top candidates from topological analysis, query vector similarity.
    - Calculate and sort by HTR score.
    - Update suggestion meta to include vectorScore and topologicalScore.
  </action>
  <verify>
    <automated>npx vitest run tests/core/LinkPredictionEngine.test.ts</automated>
  </verify>
  <done>Hybrid scoring (HTR) implemented and correctly weighting semantic signals.</done>
</task>

<task type="auto">
  <name>Hybrid Prediction Validation</name>
  <files>tests/core/LinkPredictionEngine.test.ts</files>
  <action>
    Update/Add tests in tests/core/LinkPredictionEngine.test.ts:
    - Mock SmartConnectionsAdapter to return high scores for specific note pairs.
    - Verify that these pairs rank higher in predictLinks results compared to purely topological metrics.
    - Verify graceful degradation when Smart Connections is unavailable.
  </action>
  <verify>
    <automated>npx vitest run tests/core/LinkPredictionEngine.test.ts</automated>
  </verify>
  <done>HTR logic verified with integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LinkPredictionEngine -> SmartConnectionsAdapter | Internal call to external plugin bridge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service | SmartConnectionsAdapter | mitigate | Use size-capped fallback reads and avoid querying SC API for every single node in large vaults. |
</threat_model>

<verification>
Check that suggestions in the dashboard now show higher confidence for semantically related notes even if they share few common neighbors.
</verification>

<success_criteria>
LinkPredictionEngine successfully merges vector and topological scores.
</success_criteria>

<output>
After completion, create .planning/phases/07-ai-tribunal-similarity-analysis/07-02-SUMMARY.md
</output>
