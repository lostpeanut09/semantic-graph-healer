---
phase: 07-ai-tribunal-similarity-analysis
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/core/LlmService.ts, tests/core/LlmService.test.ts, src/core/LinkPredictionEngine.ts, tests/core/LinkPredictionEngine.test.ts]
autonomous: true
requirements: [AI-01, AI-03]
must_haves:
  truths:
    - "Users see combined link suggestion scores that reflect both their structural configuration and semantic similarity."
    - "Users experience faster analysis when the primary AI is highly confident, as secondary checks are skipped."
    - "Users see a clear visual 'CONFLICT' warning with a 1-100 confidence score when the primary and secondary AI disagree."
    - "Users can read the distinct reasoning from both the primary and secondary AI models in the suggestion details."
  artifacts:
    - path: "src/core/LlmService.ts"
      provides: "AI Tribunal Consensus logic with Uncertainty Triage"
    - path: "src/core/LinkPredictionEngine.ts"
      provides: "Vector-Topological Merging (HTR)"
  key_links:
    - from: "src/core/LlmService.ts"
      to: "src/core/LlmService.ts"
      via: "Primary/Secondary query fallback logic"
      pattern: "queryPrimary.*querySecondary"
---

<objective>
Implement Core Tribunal Logic and Harmonized Topological Ranking (HTR).

Purpose: To execute the Vector-Topological Merging and the AI Consensus algorithm, utilizing Uncertainty Triage to optimize API calls.    
Output: Fully functional `LlmService` and `LinkPredictionEngine` that respect user configurations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ai-tribunal-similarity-analysis/07-CONTEXT.md
@.planning/phases/07-ai-tribunal-similarity-analysis/07-RESEARCH.md
<interfaces>
From src/types.ts:
```typescript
export interface PluginSettings {
  // Configs defined in Wave 1
  primaryModel: string;
  secondaryModel: string;
  safeZoneThreshold: number;
  htrStructuralWeight: number;
}
export interface ReasoningResult {
  verdict: 'STABLE' | 'CONFLICT';
  confidenceScore: number;
  primaryReasoning: string;
  secondaryReasoning?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Vector-Topological Merging (HTR)</name>
  <files>src/core/LinkPredictionEngine.ts, tests/core/LinkPredictionEngine.test.ts</files>
  <behavior>
    - Test 1: HTR score is calculated correctly using `htrStructuralWeight`.
    - Test 2: Structural metrics are normalized to the same scale as vector metrics (0-1 or 1-100) before merging.
  </behavior>
  <action>Implement the Harmonized Topological Ranking (HTR) in `LinkPredictionEngine`. Average structural graph metrics (like Jaccard) and semantic vector similarity using `settings.htrStructuralWeight`. Ensure both scores are normalized before blending.</action>
  <verify>
    <automated>npm test -- tests/core/LinkPredictionEngine.test.ts</automated>
  </verify>
  <done>HTR logic correctly blends structural and semantic scores based on configurable weights.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AI Tribunal Consensus Logic</name>
  <files>src/core/LlmService.ts, tests/core/LlmService.test.ts</files>
  <behavior>
    - Test 1: Secondary model is NOT called if Primary confidence >= `safeZoneThreshold`.
    - Test 2: Secondary model is called if Primary confidence < `safeZoneThreshold`.
    - Test 3: Conflicts result in `verdict: 'CONFLICT'`, `confidenceScore` mapping, and both reasonings stored in the result.
  </behavior>
  <action>Implement Uncertainty Triage in `LlmService`. Query the primary model first. If its confidence exceeds `settings.safeZoneThreshold`, return `STABLE`. Otherwise, query the secondary model. If they disagree, do NOT hide the suggestion; return a `CONFLICT` verdict, calculate a unified 1-100 `confidenceScore`, and include both `primaryReasoning` and `secondaryReasoning` in the `ReasoningResult`.</action> 
  <verify>
    <automated>npm test -- tests/core/LlmService.test.ts</automated>
  </verify>
  <done>Consensus algorithm strictly follows Safe Zone triage and resolves conflicts transparently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> LlmService | Responses from LLM providers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information Disclosure | LlmService | mitigate | Ensure API errors or rate limit messages from secondary models are handled gracefully and not directly exposed as reasoning. |
</threat_model>

<verification>
`npm test` passes for both LinkPredictionEngine and LlmService.
</verification>

<success_criteria>
Core Tribunal logic successfully implements Uncertainty Triage and returns complete ReasoningResult objects.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-tribunal-similarity-analysis/07-02-SUMMARY.md`
</output>