---
phase: 07-ai-tribunal-similarity-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    [src/types.ts, src/views/sections/PrimaryModelSettings.ts, src/views/sections/TribunalSettings.ts, docs/PLAN.md]
autonomous: true
requirements: [AI-01, AI-03]
must_haves:
    truths:
        - 'User can explicitly select their Primary and Secondary models.'
        - 'User can configure the Safe Zone threshold for Uncertainty Triage.'
        - 'User can configure the HTR structural weight for Vector-Topological Merging.'
    artifacts:
        - path: 'src/types.ts'
          provides: 'Updated configuration interfaces for Tribunal and HTR settings'
        - path: 'src/views/sections/PrimaryModelSettings.ts'
          provides: 'UI inputs for explicitly selecting Primary and Secondary models'
        - path: 'src/views/sections/TribunalSettings.ts'
          provides: 'UI inputs for Safe Zone threshold and HTR structural weight'
    key_links:
        - from: 'src/views/sections/PrimaryModelSettings.ts'
          to: 'src/types.ts'
          via: 'PluginSettings interface update'
          pattern: 'settings.primaryModel'
---

<objective>
Implement Dynamic Tribunal Selection and Uncertainty Triage Sensitivity configuration.

Purpose: To allow users to explicitly choose primary and secondary models and configure the "Safe Zone" threshold and HTR structural weight as per user decisions.
Output: Updated Types and Settings UI sections.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ai-tribunal-similarity-analysis/07-CONTEXT.md
@.planning/phases/07-ai-tribunal-similarity-analysis/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Update Council Workflow Plan</name>
  <files>docs/PLAN.md</files>
  <action>Update docs/PLAN.md with the scope of Phase 7, acceptance criteria from CONTEXT.md, and vitest command for testing.</action>    
  <verify>
    <automated>grep -c "Phase 7" docs/PLAN.md</automated>
  </verify>
  <done>docs/PLAN.md updated for Council workflow.</done>
</task>

<task type="auto">
  <name>Task 1: Update Types for Tribunal and HTR Configuration</name>
  <files>src/types.ts</files>
  <action>Update the `PluginSettings` interface to include explicit primary and secondary model configurations (e.g., `primaryModel`, `secondaryModel`, or expanding an existing array/object structure to clearly distinguish them). Add `safeZoneThreshold` (number) and `htrStructuralWeight` (number, default > 0.5 for structural priority) to the settings. Export new types `ReasoningResult` which should include `verdict` (STABLE/CONFLICT), `confidenceScore` (1-100), `primaryReasoning`, and `secondaryReasoning`.</action>
  <verify>
    <automated>grep -c "safeZoneThreshold" src/types.ts</automated>
  </verify>
  <done>Types correctly reflect the new configuration fields required for Dynamic Tribunal Selection, Safe Zone, and HTR.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tribunal and Settings UI</name>
  <files>src/views/sections/PrimaryModelSettings.ts, src/views/sections/TribunalSettings.ts</files>
  <action>Modify the PrimaryModelSettings section to allow explicit selection of BOTH a Primary model and a Secondary model from available providers (no automatic heuristics). Additionally, modify the TribunalSettings section to include inputs for the "Safe Zone threshold" (confidence level at which the secondary model is skipped) and the "HTR structural weight" (used to blend semantic vectors and structural graph metrics). Ensure all inputs map directly to the fields added in `src/types.ts`.</action>
  <verify>
    <automated>grep -E -c "(Secondary|Safe Zone)" src/views/sections/PrimaryModelSettings.ts src/views/sections/TribunalSettings.ts</automated>
  </verify>
  <done>User can explicitly assign LLM providers and configure the Safe Zone threshold and HTR structural weight via the plugin settings tab.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                    |
| --------------------------- | ---------------------------------------------- |
| Settings UI -> Plugin State | User inputs for model selection and thresholds |

## STRIDE Threat Register

| Threat ID | Category  | Component        | Disposition | Mitigation Plan                                                                            |
| --------- | --------- | ---------------- | ----------- | ------------------------------------------------------------------------------------------ |
| T-07-01   | Tampering | TribunalSettings | mitigate    | Validate inputs (e.g., threshold between 0-100, weight between 0-1) before saving to state |

</threat_model>

<verification>
Ensure tests related to settings still pass.
</verification>

<success_criteria>
Types are updated and Settings UI contains explicit configuration for Primary/Secondary models, Safe Zone, and HTR weight.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-tribunal-similarity-analysis/07-01-SUMMARY.md`
</output>
