---
phase: 07-ai-tribunal-similarity-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types.ts, src/core/LlmService.ts, tests/core/LlmService.test.ts]
autonomous: true
requirements: [AI-01]
must_haves:
  truths:
    - "LlmService can orchestrate calls to a secondary model for verification."
    - "Consensus logic correctly identifies STABLE, CONFLICT, and UNCERTAIN states."
    - "Tribunal is skipped if primary model confidence exceeds the triage threshold."
  artifacts:
    - path: "src/core/LlmService.ts"
      provides: "ConsensusController and dual-LLM orchestration"
    - path: "tests/core/LlmService.test.ts"
      provides: "Automated verification of tribunal logic"
  key_links:
    - from: "src/core/LlmService.ts"
      to: "src/types.ts"
      via: "TribunalStatus enum"
---

<objective>
Implement the AI Tribunal (dual-LLM consensus) core logic in LlmService. This provides a robust verification layer to eliminate hallucinations in high-stakes reasoning.

Purpose: Increase reasoning reliability via cross-model consistency.
Output: Enhanced LlmService with consensus orchestration and Uncertainty Triage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types.ts
@src/core/LlmService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Define Tribunal Types & Contracts</name>
  <files>src/types.ts</files>
  <behavior>
    - Define TribunalStatus enum: STABLE, CONFLICT, UNCERTAIN.
    - Add tribunalStatus and secondaryOutput to ReasoningResult interface.
    - Add aiConfidenceThreshold to SemanticGraphHealerSettings if missing.
  </behavior>
  <action>
    Update src/types.ts:
    - Export enum TribunalStatus { STABLE = 'STABLE', CONFLICT = 'CONFLICT', UNCERTAIN = 'UNCERTAIN' }.
    - Update ReasoningResult: add optional tribunalStatus: TribunalStatus; and tribunalAudit?: string;.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Types updated and project compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Implement Consensus Controller in LlmService</name>
  <files>src/core/LlmService.ts</files>
  <behavior>
    - If enableAiTribunal is true and Primary result has confidence < aiConfidenceThreshold:
      - Call Secondary model.
      - Compare Winners.
      - Return status STABLE if they match, CONFLICT if they differ, UNCERTAIN if either fails to parse.
    - Handle Secondary model failure gracefully (fallback to Primary but mark as UNCERTAIN).
  </behavior>
  <action>
    Refactor LlmService.callLlm:
    - Implement formal "Uncertainty Triage" check.
    - Use secondaryApiKey from settings/keychain.
    - Implement evaluateConsensus(primary: ReasoningResult, secondary: ReasoningResult): TribunalStatus.
    - Ensure audit tags are appended to the response only when tribunal is active.
  </action>
  <verify>
    <automated>npx vitest run tests/core/LlmService.test.ts</automated>
  </verify>
  <done>Consensus orchestration implemented with triage threshold support.</done>
</task>

<task type="auto">
  <name>Hardening LlmService Tests</name>
  <files>tests/core/LlmService.test.ts</files>
  <action>
    Add test cases to tests/core/LlmService.test.ts:
    - Test triage: Tribunal NOT called if primary score is 95%.
    - Test triage: Tribunal IS called if primary score is 60%.
    - Test consensus: Returns STABLE when models agree on Winner.
    - Test consensus: Returns CONFLICT when models disagree on Winner.
    - Test error handling: Returns UNCERTAIN if secondary model times out.
  </action>
  <verify>
    <automated>npx vitest run tests/core/LlmService.test.ts</automated>
  </verify>
  <done>All tribunal scenarios covered by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LlmService -> Secondary API | External call to potentially different provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | LlmService | mitigate | Redact note content before sending to secondary model as per primary model pattern. |
| T-07-02 | Denial of Service | LlmService | mitigate | Implement "Uncertainty Triage" to avoid doubling API calls for high-confidence results. |
</threat_model>

<verification>
Verify that LlmService correctly identifies conflicts by mocking different LLM responses in vitest.
</verification>

<success_criteria>
LlmService correctly handles multi-model consensus and respects the triage threshold.
</success_criteria>

<output>
After completion, create .planning/phases/07-ai-tribunal-similarity-analysis/07-01-SUMMARY.md
</output>
