---
phase: 13-linting-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.config/eslint.config.js, .husky/pre-commit, .husky/pre-push]
autonomous: true
requirements: [HARDEN-04, HARDEN-06]

must_haves:
  truths:
    - "ESLint recognizes Svelte 5 runes ($state, $derived, etc.) as valid globals."
    - "Husky hooks are configured to run lint/format on commit and build/test on push."
    - "Scripts directory is correctly scoped for Node.js environment without browser globals."
  artifacts:
    - path: ".config/eslint.config.js"
      provides: "Svelte 5 support and Node.js scoped overrides"
    - path: ".husky/pre-commit"
      provides: "Quality gate for local commits"
    - path: ".husky/pre-push"
      provides: "Build safety gate for remote pushes"
---

<objective>
Establish the technical foundation for repository hardening by fixing the linter configuration for Svelte 5 and Node.js scripts, and upgrading Husky hooks to ensure consistent quality.

Purpose: Prevent regression and ensure all developers follow the same strict standards.
Output: Updated ESLint config and optimized Husky hooks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@.config/eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ESLint Config for Svelte 5 and Scoped Scripts</name>
  <files>.config/eslint.config.js</files>
  <action>
    - Add Svelte 5 runes ($state, $derived, $effect, $props, $inspect, $host) to `languageOptions.globals` in the general `.ts` override section.
    - Refine the `scripts/**/*.ts` override:
      - Set `globals` to `node: true` equivalent (include `process`, `__dirname`, `__filename`, `require`, `module`).
      - Disable browser-only globals (window, document, navigator) for scripts.
      - Ensure `no-console` is 'off' for scripts.
    - Fix the `import/no-nodejs-modules` error if it persists.
  </action>
  <verify>
    <automated>npm run lint src/views/dashboard/DashboardStore.svelte.ts scripts/generate-mock-vault.ts</automated>
  </verify>
  <done>Svelte 5 runes are recognized, and scripts are properly linted for Node.js environment.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Husky Hooks for CI/CD Alignment</name>
  <files>.husky/pre-commit, .husky/pre-push</files>
  <action>
    - Update `.husky/pre-commit`:
      - Run `npm run lint:fix` (only on staged files if possible, otherwise full project).
      - Run `npm run format`.
      - Remove `npm run build` and `npm test` from pre-commit to keep it fast.
    - Update `.husky/pre-push`:
      - Run `npm run build` (TSC + esbuild).
      - Run `npm test`.
    - Ensure both scripts have correct exit codes to block the git operation on failure.
  </action>
  <verify>
    <automated>ls .husky/pre-commit .husky/pre-push && grep "npm run build" .husky/pre-push</automated>
  </verify>
  <done>Husky hooks enforce linting on commit and build/test on push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Git -> Remote | Husky hooks act as the final local filter before code reaches the shared repository. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | .husky hooks | mitigate | Ensure hooks are executable and part of the repository; use `husky` package to manage them. |
</threat_model>

<verification>
Run `npm run lint` and verify no errors in Svelte files or scripts.
Try a test commit/push (simulated) to verify hooks fire.
</verification>

<success_criteria>
- ESLint: zero errors for `$state` and other runes.
- Husky: pre-push hook includes `npm run build`.
</success_criteria>

<output>
After completion, create `.planning/phases/13-linting-hardening/13-01-SUMMARY.md`
</output>
