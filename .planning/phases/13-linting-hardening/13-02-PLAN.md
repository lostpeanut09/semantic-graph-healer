---
phase: 13-linting-hardening
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [src/main.ts, src/views/components/*.ts, src/views/sections/*.ts, src/core/adapters/*.ts, src/core/utils/HealerLogger.ts]
autonomous: true
requirements: [HARDEN-04, HARDEN-06]

must_haves:
  truths:
    - "UI strings in settings and commands follow Obsidian's Sentence case (e.g., 'Open graph view')."
    - "Project has zero 'unused variable' or 'missing await' warnings."
    - "Floating promises are handled or explicitly marked as ignored."
  artifacts:
    - path: "src/main.ts"
      provides: "Compliant command labels"
    - path: "src/views/sections/CoreSettings.ts"
      provides: "Compliant settings labels"
---

<objective>
Align the User Interface with Obsidian's Human Interface Guidelines and perform a general cleanup of common lint warnings to achieve a high-quality codebase.

Purpose: Improve UX professionalism and code hygiene.
Output: Sentence case compliant UI and zero basic lint warnings.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main.ts
@src/views/sections/CoreSettings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk UI Sentence Case Correction</name>
  <files>src/main.ts, src/views/components/*.ts, src/views/sections/*.ts, src/views/dashboard/components/*.svelte</files>
  <action>
    - Systematically update all setting names, descriptions, command names, and button labels to use **Sentence case**.
    - Key targets: `src/main.ts` commands, `ConfirmationModal.ts`, `GraphPopup.ts`, and all files in `src/views/sections/`.
    - Follow all `obsidianmd/ui/sentence-case` warnings from the latest lint report.
  </action>
  <verify>
    <automated>npm run lint | grep "sentence-case" | wc -l</automated>
  </verify>
  <done>Zero sentence-case warnings remain.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate Common Lint Warnings (Unused Vars, Missing Awaits, Console)</name>
  <files>src/core/adapters/*.ts, src/core/utils/HealerLogger.ts, src/views/*.ts</files>
  <action>
    - Fix all `@typescript-eslint/no-unused-vars` by removing the variables or prefixing with `_`.
    - Fix all `@typescript-eslint/require-await` by removing `async` where unnecessary or adding `await`.
    - Fix all `@typescript-eslint/no-floating-promises` by adding `await`, `.catch()`, or `void` prefix.
    - Resolve the `no-console` warning in `HealerLogger.ts` by ensuring only allowed methods are used or using a proper override for the logger core.
  </action>
  <verify>
    <automated>npm run lint | grep -E "no-unused-vars|require-await|no-floating-promises|no-console" | wc -l</automated>
  </verify>
  <done>Zero basic hygiene warnings remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> User | Sentence case ensures the plugin feels native to the Obsidian ecosystem. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | HealerLogger.ts | mitigate | Ensure no-console rule prevents accidental logging of sensitive metadata to the production console. |
</threat_model>

<verification>
Run `npm run lint`. The number of problems should be significantly reduced, ideally only 'any' related warnings remain.
</verification>

<success_criteria>
- Zero `sentence-case` warnings.
- Zero `no-unused-vars` warnings.
- Zero `require-await` warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/13-linting-hardening/13-02-SUMMARY.md`
</output>
