---
phase: 13-linting-hardening
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified: [src/core/workers/graph-analysis-core.ts, src/views/dashboard/DashboardStore.svelte.ts, src/views/GraphVisualizerView.ts, src/types.ts]
autonomous: true
requirements: [HARDEN-04, HARDEN-05]

must_haves:
  truths:
    - "Worker message passing is strictly typed with union types instead of 'any'."
    - "Dashboard store uses proper interfaces for suggestions and plugin context."
    - "Graph visualizer view defines strict types for the underlying force-graph instance."
  artifacts:
    - path: "src/core/workers/graph-analysis-core.ts"
      provides: "Strictly typed worker communication"
    - path: "src/views/dashboard/DashboardStore.svelte.ts"
      provides: "Strictly typed Svelte 5 store"
---

<objective>
Eliminate the usage of 'any' and unsafe type operations in critical core services and views, achieving full type safety across the application.

Purpose: Improve reliability, developer experience, and compliance with Obsidian review standards.
Output: Zero 'any' related lint warnings and a strictly typed core.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/workers/graph-analysis-core.ts
@src/views/dashboard/DashboardStore.svelte.ts
@src/views/GraphVisualizerView.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve 'any' in Core Workers and Communication</name>
  <files>src/core/workers/graph-analysis-core.ts, src/core/services/GraphWorkerService.ts</files>
  <action>
    - Define `WorkerAction` and `WorkerPayload` interfaces/unions.
    - Replace `any` in `onmessage` and `postMessage` calls with these types.
    - Fix unsafe assignments and member accesses in `graph-analysis-core.ts`.
    - Use type guards or `unknown` where external data is received.
  </action>
  <verify>
    <automated>npm run lint src/core/workers/graph-analysis-core.ts</automated>
  </verify>
  <done>Worker communication is fully typed without 'any'.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Dashboard and Graph Visualizer Types</name>
  <files>src/views/dashboard/DashboardStore.svelte.ts, src/views/GraphVisualizerView.ts, src/views/dashboard/components/*.svelte</files>
  <action>
    - `DashboardStore`: Replace `any` with strict interfaces for `Plugin` context and `Suggestion` objects.
    - `GraphVisualizerView`: 
      - Define a type/interface for the `ForceGraph3D` instance (can be partial if full type is unavailable).
      - Replace `any` in node/link callback arguments with strict interfaces.
    - Ensure all Svelte components receive correctly typed props.
  </action>
  <verify>
    <automated>npm run lint src/views/dashboard/DashboardStore.svelte.ts src/views/GraphVisualizerView.ts</automated>
  </verify>
  <done>Dashboard and Graph UI are strictly typed with zero 'any' warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Main Thread -> Worker | Typed messages prevent malformed data from causing crashes in the background thread. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Denial of Service | Web Worker | mitigate | Strict typing prevents processing of invalid payloads that could lead to infinite loops or worker crashes. |
</threat_model>

<verification>
Run `npm run lint` and `npm run build`. Both must pass with zero warnings and zero errors.
This is the final verification for the phase.
</verification>

<success_criteria>
- ESLint output: "0 problems".
- TSC output: No errors.
- Zero `no-explicit-any` warnings in the entire `src/` directory.
</success_criteria>

<output>
After completion, create `.planning/phases/13-linting-hardening/13-03-SUMMARY.md`
</output>
