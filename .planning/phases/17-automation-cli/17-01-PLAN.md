---
phase: 17-automation-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types.ts, src/core/services/AutomationApi.ts, src/main.ts, tests/core/AutomationApi.test.ts]
autonomous: true
requirements: [REQ-17.1, REQ-17.2, REQ-17.3, REQ-17.4]

must_haves:
    truths:
        - 'AutomationApi service exists and is instantiated in main.ts'
        - 'CLI commands healer:scan, healer:suggestions, healer:metrics are registered'
        - 'CLI commands return valid JSON output to stdout/console'
    artifacts:
        - path: 'src/core/services/AutomationApi.ts'
          provides: 'Core logic for headless automation'
        - path: 'src/main.ts'
          provides: 'Public API exposure and CLI handler registration'
    key_links:
        - from: 'src/main.ts'
          to: 'src/core/services/AutomationApi.ts'
          via: 'this.api instantiation'
---

<objective>
Implement the core Automation API and register Obsidian CLI subcommands to enable headless graph analysis and repair.

Purpose: Allow power users and scripts to interact with the Healer via terminal (obsidian-cli).
Output: A new AutomationApi service and registered CLI handlers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-CONTEXT.md
@.planning/phases/17-RESEARCH.md
@src/main.ts
@src/types.ts
@src/core/SuggestionExecutor.ts
@src/core/GraphEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Automation Interfaces</name>
  <files>src/types.ts</files>
  <action>
    - Define `HealerApi` interface with methods: `runAnalysis`, `getSuggestions`, `getMetrics`, `executeFix`, `executeBatch`.
    - Define `AutomationMetrics` interface to include nodes, edges, density, and topological scores.
    - Add `CliData` and `CliFlags` types if not already present (per Obsidian API).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Types for Automation API are defined and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HealerAutomationApi Service</name>
  <files>src/core/services/AutomationApi.ts, tests/core/AutomationApi.test.ts</files>
  <action>
    - Create `HealerAutomationApi` class in `src/core/services/AutomationApi.ts`.
    - Implement `runAnalysis(options: { silent: boolean })`: Wrapper around `plugin.analyzeGraph`.
    - Implement `getSuggestions()`: Returns `cache.suggestions` array.
    - Implement `getMetrics()`: Returns health metrics, graph size, and topological scores from `graphEngine`.
    - Implement `executeFix(id: string)`: Finds suggestion by ID and calls `executor.execute`.
    - Implement `executeBatch(options: { confidence: number, category?: string })`: Filters suggestions and executes them. Default confidence: 0.9.
    - Create unit tests in `tests/core/AutomationApi.test.ts` to verify logic.
  </action>
  <verify>
    <automated>npx vitest tests/core/AutomationApi.test.ts</automated>
  </verify>
  <done>Automation logic is encapsulated and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Register CLI Handlers and Expose Public API</name>
  <files>src/main.ts</files>
  <action>
    - In `SemanticGraphHealer.onload()`: Instantiate `HealerAutomationApi` and assign to `this.api`.
    - Register CLI handlers using `this.registerCliHandler(command, description, flags, handler)`:
        - `healer:scan`: Runs analysis and returns JSON status.
        - `healer:suggestions`: Returns `getSuggestions()` as stringified JSON.
        - `healer:metrics`: Returns `getMetrics()` as stringified JSON.
        - `healer:apply-batch`: Runs `executeBatch()` and returns results.
    - Ensure all handlers use `JSON.stringify()` for output and handle `Promise` returns.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Plugin exposes API and subcommands are available to the Obsidian CLI binary.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                                    |
| ------------- | -------------------------------------------------------------- |
| CLI -> Plugin | Commands received from the OS environment via Obsidian binary. |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                          |
| --------- | ---------------------- | ---------------------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-17-01   | Tampering              | executeBatch params    | mitigate    | Validate `confidence` threshold and `category` using strict typing/Zod in the handler.   |
| T-17-02   | Information Disclosure | getSuggestions/metrics | accept      | CLI access requires local machine access to the vault; standard Obsidian security model. |
| T-17-SC   | Tampering              | npm installs           | mitigate    | No new npm packages introduced in this phase.                                            |

</threat_model>

<verification>
Check if `obsidian eval` can access `app.plugins.plugins['semantic-graph-healer'].api`.
Verify CLI subcommand registration via mock tests.
</verification>

<success_criteria>

- `npm run build` passes.
- Unit tests for `AutomationApi` pass.
- `main.ts` contains `registerCliHandler` for all required commands.
  </success_criteria>

<output>
Create `.planning/phases/17-automation-cli/17-01-SUMMARY.md` when done
</output>
