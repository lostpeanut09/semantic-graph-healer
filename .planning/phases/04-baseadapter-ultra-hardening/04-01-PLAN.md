---
phase: 04-baseadapter-ultra-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/adapters/IMetadataAdapter.ts, src/core/adapters/BaseAdapter.ts]
autonomous: true
requirements: [HARDEN-03d, HARDEN-03e]
must_haves:
  truths:
    - "IMetadataAdapter methods use explicit return types instead of implicit any"
    - "BaseAdapter implements an async initialize() method with initPromise guard"
    - "BaseAdapter provides an ensureInitialized() method that throws if called prematurely"
  artifacts:
    - path: "src/core/adapters/IMetadataAdapter.ts"
      provides: "Strictly typed interface for adapters"
    - path: "src/core/adapters/BaseAdapter.ts"
      provides: "Common initialization guards for all adapters"
  key_links:
    - from: "BaseAdapter.ensureInitialized"
      to: "this.initialized"
      via: "Boolean check and throw"
---

<objective>
Harden the core adapter architecture by enforcing strict type-safety and implementing asynchronous initialization guards to prevent race conditions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-baseadapter-ultra-hardening/04-CONTEXT.md
@.planning/phases/04-baseadapter-ultra-hardening/04-RESEARCH.md
@src/core/adapters/IMetadataAdapter.ts
@src/core/adapters/BaseAdapter.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parametrize IMetadataAdapter for strict type safety</name>
  <files>src/core/adapters/IMetadataAdapter.ts</files>
  <behavior>
    - All methods returning Promises must have explicit generic parameters (e.g., `Promise<SemanticLinkEdge[]>`).
    - Methods like `getPage` and `getPages` must return `DataviewPage` types explicitly defined in types.ts.
    - No `any` should be used for return types or parameters where specific types exist.
  </behavior>
  <action>
    Update IMetadataAdapter.ts (per D-05) to ensure all Promise-based methods are strictly typed.
    - `getLinks()`: `Promise<SemanticLinkEdge[]>`
    - `getLinksSafe()`: `Promise<SemanticLinkEdge[]>`
    - `queryPages(query: string)`: `Promise<DataviewPage[]>`
    - `getHierarchy(path: string)`: `Promise<HierarchyNode | null>`
    - `getRelatedNotes(path: string, limit: number)`: `Promise<RelatedNote[]>`
    Ensure imports for `HierarchyNode` and `RelatedNote` are correct.
  </action>
  <verify>
    <automated>npm run lint -- src/core/adapters/IMetadataAdapter.ts</automated>
  </verify>
  <done>Interface is strictly typed and passes linting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Async Initialization Guards in BaseAdapter</name>
  <files>src/core/adapters/BaseAdapter.ts, tests/core/adapters/BaseAdapter.test.ts</files>
  <behavior>
    - `BaseAdapter` must have a private `initialized` boolean (default false).
    - `BaseAdapter` must have a private `initPromise` of type `Promise<void> | null`.
    - `initialize()` method must return the `initPromise` if it exists, otherwise create it and call `onInitialize()`.
    - `ensureInitialized()` must throw an Error "Adapter [id] not initialized" if `initialized` is false.
  </behavior>
  <action>
    Refactor BaseAdapter.ts (per D-04):
    1. Add `protected initialized = false;`.
    2. Add `private initPromise: Promise<void> | null = null;`.
    3. Implement `public async initialize(): Promise<void>` following the pattern in RESEARCH.md.
    4. Define `protected abstract onInitialize(): Promise<void>;` that subclasses must implement.
    5. Implement `protected ensureInitialized(): void` that throws "Fail Loudly" per CONTEXT.md.
  </action>
  <verify>
    <automated>npm test tests/core/adapters/BaseAdapter.test.ts</automated>
  </verify>
  <done>Initialization logic and guards are implemented and tested in the base class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Adapter Implementation → BaseAdapter | Subclasses must respect the initialization protocol |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | BaseAdapter | mitigate | Use `ensureInitialized()` guard in all critical methods to prevent state corruption. |
</threat_model>

<success_criteria>
- IMetadataAdapter is fully parameterized.
- BaseAdapter supports concurrent `initialize()` calls via `initPromise`.
- `ensureInitialized()` throws if called before completion.
</success_criteria>

<output>
After completion, create `.planning/phases/04-baseadapter-ultra-hardening/04-01-SUMMARY.md`
</output>
