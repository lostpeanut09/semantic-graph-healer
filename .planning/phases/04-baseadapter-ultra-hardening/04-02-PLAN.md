---
phase: 04-baseadapter-ultra-hardening
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/core/adapters/NativeVaultAdapter.ts]
autonomous: true
requirements: [HARDEN-03b, HARDEN-03d]
must_haves:
  truths:
    - "NativeVaultAdapter skips self-links and non-file targets"
    - "All paths emitted by NativeVaultAdapter are normalized"
    - "NativeVaultAdapter throws if getLinks is called before initialization"
  artifacts:
    - path: "src/core/adapters/NativeVaultAdapter.ts"
      provides: "Hardened native metadata extraction"
  key_links:
    - from: "NativeVaultAdapter.getLinks"
      to: "HealerUtils.normalizeVaultPath"
      via: "Function call for every edge"
---

<objective>
Harden NativeVaultAdapter by implementing strict path normalization, filtering out invalid edges (self-links, non-files), and applying initialization guards.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-baseadapter-ultra-hardening/04-01-SUMMARY.md
@src/core/adapters/NativeVaultAdapter.ts
@src/core/HealerUtils.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement onInitialize and Guards in NativeVaultAdapter</name>
  <files>src/core/adapters/NativeVaultAdapter.ts</files>
  <behavior>
    - Implement `onInitialize()` (empty or for future setup).
    - Call `this.ensureInitialized()` at the start of `getLinks()`, `getPage()`, `queryPages()`, etc.
  </behavior>
  <action>
    Update NativeVaultAdapter.ts to follow the new BaseAdapter protocol:
    1. Implement `protected async onInitialize(): Promise<void> { return Promise.resolve(); }`.
    2. Add `this.ensureInitialized()` calls to all public methods.
  </action>
  <verify>
    <automated>npm test tests/core/adapters/BaseAdapter.test.ts</automated>
  </verify>
  <done>Initialization protocol implemented in NativeVaultAdapter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden Path Extraction and Filtering</name>
  <files>src/core/adapters/NativeVaultAdapter.ts, tests/core/adapters/BaseAdapter.test.ts</files>
  <behavior>
    - Source and target paths must be passed through `HealerUtils.normalizeVaultPath`.
    - Skip edges where `source === target`.
    - Skip targets that are not files (e.g., folders if they appear in resolvedLinks).
    - Respect `this.app.settings.includeNonMarkdownHubs` (via plugin settings) to filter by extension.
  </behavior>
  <action>
    Refactor `getLinks()` in NativeVaultAdapter.ts (per HARDEN-03b and CONTEXT.md):
    1. Use `this.app.metadataCache.resolvedLinks`.
    2. Normalize both source and target using `normalizeVaultPath(this.app, path, context)`.
    3. Filter out self-links.
    4. Implement "Hybrid Filtering": if `includeNonMarkdownHubs` is false, only include targets with `.md` extension.
    5. Access settings via `this.app.plugins.getPlugin("semantic-graph-healer").settings` (or similar).
  </action>
  <verify>
    <automated>npm test tests/core/adapters/BaseAdapter.test.ts</automated>
  </verify>
  <done>NativeVaultAdapter extracts clean, normalized, and filtered edges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Vault Metadata → Graph | Raw linktext from Obsidian must be normalized before entering the graph |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Info Disclosure | NativeVaultAdapter | mitigate | Use `normalizeVaultPath` to ensure edges point to valid, internal vault locations. |
</threat_model>

<success_criteria>
- NativeVaultAdapter passes normalization tests.
- Self-links are excluded.
- Non-markdown files are excluded by default (unless setting enabled).
</success_criteria>

<output>
After completion, create `.planning/phases/04-baseadapter-ultra-hardening/04-02-SUMMARY.md`
</output>
