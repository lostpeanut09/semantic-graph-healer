---
phase: 04-baseadapter-ultra-hardening
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/core/adapters/SmartConnectionsAdapter.ts, src/types.ts, src/views/sections/IntegrationsSettings.ts]
autonomous: true
requirements: [HARDEN-03g]
must_haves:
  truths:
    - "SmartConnectionsAdapter implements an AJSON size cap to prevent UI blocking"
    - "SmartConnectionsAdapter implements an early break for large vaults"
    - "The plugin settings allow configuring the AJSON size cap"
  artifacts:
    - path: "src/core/adapters/SmartConnectionsAdapter.ts"
      provides: "Optimized RAG fallback"
    - path: "src/types.ts"
      provides: "Updated settings interface"
    - path: "src/views/sections/IntegrationsSettings.ts"
      provides: "User controls for hardening optimizations"
---

<objective>
Optimize SmartConnectionsAdapter for large vaults by implementing size caps and early-break logic, and expose the new hardening configurations (Hybrid filtering, AJSON size) in the settings dashboard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-baseadapter-ultra-hardening/04-01-SUMMARY.md
@src/core/adapters/SmartConnectionsAdapter.ts
@src/types.ts
@src/views/sections/IntegrationsSettings.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optimize SmartConnectionsAdapter Fallback</name>
  <files>src/core/adapters/SmartConnectionsAdapter.ts</files>
  <behavior>
    - Before parsing AJSON, check the file size.
    - If size > `smartConnectionsAjsonSizeCap`, skip parsing or log a warning.
    - Implement an early break in similarity search if `limit` is reached.
    - Implement initialization guards.
  </behavior>
  <action>
    Refactor SmartConnectionsAdapter.ts (per HARDEN-03g):
    1. Implement `protected async onInitialize(): Promise<void>`.
    2. Add `this.ensureInitialized()` guards.
    3. In AJSON parsing logic, check `file.size` against `this.app.settings.smartConnectionsAjsonSizeCap`.
    4. Ensure the search loop breaks as soon as the required number of related notes is found.
  </action>
  <verify>
    <automated>npm test tests/core/adapters/SmartConnectionsAdapter.test.ts</automated>
  </verify>
  <done>SmartConnectionsAdapter is optimized for performance and resource safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose Hardening Settings in UI</name>
  <files>src/types.ts, src/views/sections/IntegrationsSettings.ts</files>
  <behavior>
    - Add `smartConnectionsAjsonSizeCap` to `SemanticGraphHealerSettings` and `DEFAULT_SETTINGS` (default 1MB).
    - Add UI toggle/input for "Broad Semantic Support" (NativeVault filtering) and "SmartConnections AJSON Size Cap".
  </behavior>
  <action>
    1. Update `src/types.ts`: Add `smartConnectionsAjsonSizeCap: number` to settings interface and defaults.
    2. Update `src/views/sections/IntegrationsSettings.ts`: Add settings controls for the new hardening options.
       - Toggle for `includeNonMarkdownHubs`.
       - Number input for `smartConnectionsAjsonSizeCap`.
  </action>
  <verify>
    <automated>npm run lint -- src/views/sections/IntegrationsSettings.ts</automated>
  </verify>
  <done>New hardening settings are configurable by the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System → SmartConnections | Parsing large AJSON files from the file system must be throttled |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service | SmartConnectionsAdapter | mitigate | Implement AJSON size caps to prevent main-thread blocking during large file parsing. |
| T-04-06 | Denial of Service | SmartConnectionsAdapter | mitigate | Implement early breaks in similarity search loops. |
</threat_model>

<success_criteria>
- Large AJSON files do not block the UI.
- New settings are visible and functional in the Integrations tab.
</success_criteria>

<output>
After completion, create `.planning/phases/04-baseadapter-ultra-hardening/04-05-SUMMARY.md`
</output>
