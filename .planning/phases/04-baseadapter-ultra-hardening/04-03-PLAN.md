---
phase: 04-baseadapter-ultra-hardening
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/core/adapters/UnifiedMetadataAdapter.ts]
autonomous: true
requirements: [HARDEN-03a, HARDEN-03c, HARDEN-03f]
must_haves:
  truths:
    - "UnifiedMetadataAdapter cleans up all EventRefs on destroy"
    - "getLinks aggregates results in parallel using Promise.all"
    - "Link deduplication uses confidence priority and metadata merging"
  artifacts:
    - path: "src/core/adapters/UnifiedMetadataAdapter.ts"
      provides: "High-performance deduplicated link aggregation"
  key_links:
    - from: "UnifiedMetadataAdapter.getLinks"
      to: "LeafAdapters.getLinksSafe"
      via: "Promise.all"
---

<objective>
Harden UnifiedMetadataAdapter by implementing robust EventRef tracking for memory leak prevention, parallelizing link extraction, and implementing a deterministic merge algorithm for link deduplication.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-baseadapter-ultra-hardening/04-01-SUMMARY.md
@src/core/adapters/UnifiedMetadataAdapter.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EventRef Cleanup in UnifiedMetadataAdapter</name>
  <files>src/core/adapters/UnifiedMetadataAdapter.ts</files>
  <behavior>
    - Store all `EventRef` objects returned by `app.metadataCache.on` in a private array.
    - Implement `onDestroy()` to iterate over the array and call `app.metadataCache.offref(ref)`.
  </behavior>
  <action>
    Refactor UnifiedMetadataAdapter.ts (per HARDEN-03a and D-01):
    1. Add `private eventRefs: EventRef[] = [];`.
    2. Update `initialize()` (or the code where listeners are registered) to push refs into `eventRefs`.
    3. Implement `protected onDestroy(): void` to unregister all listeners.
  </action>
  <verify>
    <automated>npm test tests/core/adapters/UnifiedMetadataAdapter.test.ts</automated>
  </verify>
  <done>Memory leaks from event listeners are prevented by explicit unregistration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parallel Link Aggregation and Deduplication</name>
  <files>src/core/adapters/UnifiedMetadataAdapter.ts, tests/core/adapters/UnifiedMetadataAdapter.test.ts</files>
  <behavior>
    - Use `Promise.all` to call `getLinksSafe()` on all enabled adapters.
    - Implement Map-based deduplication using key `${source}|${target}|${type}`.
    - Merge metadata (context, position) and prioritize higher confidence.
  </behavior>
  <action>
    Refactor `getLinks()` in UnifiedMetadataAdapter.ts (per HARDEN-03c, HARDEN-03f, and D-03):
    1. Parallelize calls to leaf adapters using `Promise.all`.
    2. Flatten results and populate a `Map<string, SemanticLinkEdge>`.
    3. If a link already exists in the Map:
       - Keep the one with higher `confidence`.
       - If confidence is tied, merge `context` (joining with newline) and keep the first `position`.
    4. Return `Array.from(map.values())`.
  </action>
  <verify>
    <automated>npm test tests/core/adapters/UnifiedMetadataAdapter.test.ts</automated>
  </verify>
  <done>UnifiedMetadataAdapter returns a clean, deduplicated set of links aggregated in parallel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Unified Adapter → Cache | Aggregated links must be consistent and deduplicated before caching |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | UnifiedMetadataAdapter | mitigate | Use `EventRef` cleanup to prevent memory leaks that could lead to crashes. |
</threat_model>

<success_criteria>
- Event listeners are cleaned up on shutdown.
- `getLinks` is faster due to parallelism.
- Duplicate links from multiple sources (e.g., Dataview + Breadcrumbs) are correctly merged.
</success_criteria>

<output>
After completion, create `.planning/phases/04-baseadapter-ultra-hardening/04-03-SUMMARY.md`
</output>
