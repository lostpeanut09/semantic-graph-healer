---
phase: 17-automation-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    [
        src/types.ts,
        src/core/services/Notifier.ts,
        src/core/services/PluginContext.ts,
        src/core/SuggestionExecutor.ts,
        src/core/services/AutomationApi.ts,
        tests/core/AutomationApi.test.ts,
    ]
autonomous: true
requirements: [REQ-17.1, REQ-17.2, REQ-17.4]
must_haves:
    truths:
        - 'SuggestionExecutor uses an injected Notifier service instead of direct Notice calls'
        - 'AutomationApi provides a SilentNotifier to suppress UI during headless operations'
        - 'API methods return structured JSON-compatible data with optimized cloning'
    artifacts:
        - path: 'src/core/services/Notifier.ts'
          provides: 'Notification abstraction layer'
        - path: 'src/core/services/AutomationApi.ts'
          provides: 'Programmatic API surface with SilentNotifier'
        - path: 'src/core/SuggestionExecutor.ts'
          contains: 'this.context.notifier'
    key_links:
        - from: 'src/core/SuggestionExecutor.ts'
          to: 'src/core/services/Notifier.ts'
          via: 'interface method calls'
---

<objective>
Implement the Notifier service abstraction and the core programmatic API to enable headless, silent operation without brittle manual guards.

Purpose: Decouple UI side-effects from core logic and provide a stable foundation for Obsidian CLI integration.
Output: Notifier service, Silent execution mode, and a new AutomationApi foundation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-CONTEXT.md
@.planning/phases/17-RESEARCH.md
@.planning/phases/17-automation-cli/REVIEWS.md
@src/types.ts
@src/core/SuggestionExecutor.ts
@src/core/services/PluginContext.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Notifier Service and Refactor SuggestionExecutor</name>
  <files>src/types.ts, src/core/services/Notifier.ts, src/core/services/PluginContext.ts, src/core/SuggestionExecutor.ts</files>
  <action>
    - Define `HealerNotifier` interface in `src/types.ts` with `show(message: string, type?: 'info' | 'error' | 'warning'): void`.
    - Create `src/core/services/Notifier.ts` implementing `ObsidianNotifier` (wraps `new Notice()`).
    - Add `notifier: HealerNotifier` to `ExecutionContext` in `src/core/services/PluginContext.ts`.
    - Refactor `SuggestionExecutor.ts`:
        - Inject `notifier` via context.
        - REPLACE all `new Notice(...)` calls with `this.context.notifier.show(...)`.
        - Remove manual "silent" guards if any were pre-emptively added (per REVIEWS.md HIGH severity).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>SuggestionExecutor is decoupled from the Obsidian UI Notice API.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HealerAutomationApi and SilentNotifier</name>
  <files>src/core/services/AutomationApi.ts, tests/core/AutomationApi.test.ts</files>
  <action>
    - Define `HealerAutomationApi` interface in `src/types.ts`.
    - Create `src/core/services/AutomationApi.ts` implementing the service.
    - Implement `SilentNotifier`: A private class within `AutomationApi.ts` that implements `HealerNotifier` but logs to console or a results buffer instead of showing UI.
    - Implement `runAnalysis(options: { silent: boolean })`: Uses `SilentNotifier` if `silent: true`.
    - Implement `getSuggestions()`: Returns suggestions. Optimize cloning for large vaults (e.g., shallow clone of properties needed for JSON output only).
    - Implement `getMetrics()`: Aggregates graph stats.
    - Create unit tests in `tests/core/AutomationApi.test.ts` to verify `SilentNotifier` prevents UI interactions.
  </action>
  <verify>
    <automated>npx vitest tests/core/AutomationApi.test.ts</automated>
  </verify>
  <done>Core programmatic API is functional with proper notification abstraction.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                       |
| --------------- | ------------------------------------------------- |
| API -> Notifier | Selection of UI vs Headless notification strategy |

## STRIDE Threat Register

| Threat ID | Category  | Component          | Disposition | Mitigation Plan                                                                              |
| --------- | --------- | ------------------ | ----------- | -------------------------------------------------------------------------------------------- |
| T-17-01   | Tampering | SuggestionExecutor | mitigate    | Ensure mementos are ALWAYS captured even in silent mode to allow undoing automated mistakes. |

</threat_model>

<verification>
Verify `SuggestionExecutor` calls the notifier service.
Verify `SilentNotifier` logs to console/buffer instead of UI.
</verification>

<success_criteria>

- SuggestionExecutor uses Notifier service.
- AutomationApi service exists with SilentNotifier.
- JSON cloning is efficient.
  </success_criteria>

<output>
Create `.planning/phases/17-automation-cli/17-01-SUMMARY.md` when done
</output>
