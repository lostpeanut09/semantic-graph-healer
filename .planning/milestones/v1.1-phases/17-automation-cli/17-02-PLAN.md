---
phase: 17-automation-cli
plan: 02
type: execute
wave: 2
depends_on: ['17-01']
files_modified:
    [
        src/main.ts,
        src/types.ts,
        src/core/services/AutomationApi.ts,
        tests/core/AutomationBatch.test.ts,
        tests/core/CliTerminalSimulation.test.ts,
    ]
autonomous: true
requirements: [REQ-17.3, REQ-17.4]
must_haves:
    truths:
        - 'CLI subcommands and URI protocol handlers are registered'
        - 'Batch execution supports batchId-based rollbacks'
        - 'Confidence gate audit ensures normalized behavior across engines'
        - 'CLI handlers return pure JSON even on errors'
    artifacts:
        - path: 'src/main.ts'
          contains: 'registerObsidianProtocolHandler'
        - path: 'src/core/services/AutomationApi.ts'
          contains: 'undoBatch'
    key_links:
        - from: 'src/main.ts'
          to: 'src/core/services/AutomationApi.ts'
          via: 'protocol handler and CLI bridge'
---

<objective>
Expose the Automation API via CLI, URI, and UI commands, and implement safe batch execution with atomic rollback capabilities.

Purpose: Enable terminal and app-to-app automation with safety-first batch processing.
Output: CLI handlers, URI protocol handlers, batch repair logic with rollback, and terminal simulation tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-CONTEXT.md
@.planning/phases/17-RESEARCH.md
@.planning/phases/17-automation-cli/REVIEWS.md
@.planning/phases/17-automation-cli/17-01-SUMMARY.md
@src/main.ts
@src/core/services/AutomationApi.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Normalization and Implement Batch Repairs with Rollback</name>
  <files>src/types.ts, src/core/services/AutomationApi.ts, tests/core/AutomationBatch.test.ts</files>
  <action>
    - Add `batchId?: string` to `HistoryItem` in `src/types.ts`.
    - AUDIT confidence score normalization: Verify `LinkPredictionEngine` (0-1), `TagPropagator` (0-1), and `TopologyAnalyzer` (0-1) use consistent scales.
    - Implement `executeBatch(options: { confidence: number, category?: string })` in `AutomationApi.ts`:
        - Generate a unique `batchId` (UUID).
        - Filter suggestions by threshold.
        - Inject `batchId` into the executor context so Mementos/HistoryItems are tagged.
        - Return summary including the `batchId`.
    - Implement `undoBatch(batchId: string)`: Finds all history items with the matching ID and reverts them in reverse order.
    - Add tests for batch repairs and batch-level rollback.
  </action>
  <verify>
    <automated>npx vitest tests/core/AutomationBatch.test.ts</automated>
  </verify>
  <done>Batch execution is safe, atomic-capable, and threshold-normalized.</done>
</task>

<task type="auto">
  <name>Task 2: Register Handlers (CLI/URI) and Terminal Simulation</name>
  <files>src/main.ts, tests/core/CliTerminalSimulation.test.ts</files>
  <action>
    - Register CLI handlers via `this.registerCliHandler` (v1.12+):
        - Wrap each handler in a top-level try/catch.
        - Ensure all errors are returned as JSON: `{"status": "error", "message": "..."}`.
        - Register: `healer:scan`, `healer:export-suggestions`, `healer:apply-batch`, `healer:undo-batch`.
    - Register URI handlers via `this.registerObsidianProtocolHandler`:
        - Support `obsidian://healer-action?action=scan`, `apply-batch`, etc.
    - Add UI commands for Command Palette (REQ-17.3).
    - Create `tests/core/CliTerminalSimulation.test.ts`:
        - Mock the Obsidian CLI bridge to verify that output is pure JSON and that stdout/stderr separation is respected.
    - Run quality pipeline: `npm run lint`, `npm run format`.
  </action>
  <verify>
    <automated>npx vitest tests/core/CliTerminalSimulation.test.ts</automated>
  </verify>
  <done>Automation is fully exposed via terminal and URI with robust error handling.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                               |
| ------------------ | ----------------------------------------- |
| CLI Handler -> API | OS IPC passing parameters                 |
| URI -> Plugin      | URL parameters from external applications |

## STRIDE Threat Register

| Threat ID | Category               | Component          | Disposition | Mitigation Plan                                                            |
| --------- | ---------------------- | ------------------ | ----------- | -------------------------------------------------------------------------- |
| T-17-02   | Information Disclosure | export-suggestions | accept      | Output restricted to local terminal.                                       |
| T-17-03   | Denial of Service      | apply-batch        | mitigate    | Sequential execution prevents concurrent write-locks.                      |
| T-17-04   | Tampering              | apply-batch        | mitigate    | Batch rollback via `undo-batch` provides recovery from automated mistakes. |

</threat_model>

<verification>
Verify `obsidian eval` returns JSON.
Verify `healer:undo-batch` reverts multiple changes.
Verify URI protocol handling.
</verification>

<success_criteria>

- CLI handlers registered with error wrapping.
- URI protocol handlers functional.
- BatchId support for rollbacks.
- Terminal simulation tests pass.
  </success_criteria>

<output>
Create `.planning/phases/17-automation-cli/17-02-SUMMARY.md` when done
</output>
