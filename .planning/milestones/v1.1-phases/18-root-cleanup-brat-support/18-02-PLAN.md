---
phase: 18
plan: '02'
type: 'ci'
wave: 2
depends_on: ['18-01']
files_modified: ['.github/workflows/release-brat.yml', 'README.md']
autonomous: true
requirements: ['REQ-18.3']
must_haves:
    truths:
        - 'dist branch must contain only deployment artifacts.'
    artifacts:
        - '.github/workflows/release-brat.yml'
---

# 18-02: BRAT Distribution & Documentation

**Goal:** Set up automated BRAT distribution and update installation guides.

<task type="auto">
<name>Create BRAT Distribution Workflow</name>
<files>
  <create>.github/workflows/release-brat.yml</create>
</files>
<action>
Create the GitHub Action to push ONLY deployment artifacts to the 'dist' branch:
1. Trigger on push to 'main'.
2. Build plugin using 'npm run build'.
3. Create 'dist_bundle' directory and copy: main.js, manifest.json, styles.css, worker.js, ladybug-worker.js.
4. Use s0/git-publish-subdir-action to publish 'dist_bundle' to the 'dist' branch.
</action>
<verify>
<automated>cat .github/workflows/release-brat.yml</automated>
Expected: Correct YAML configuration for selective artifact deployment via dist_bundle.
</verify>
<done>CI/CD for BRAT distribution established with clean bundle strategy.</done>
</task>

<task type="auto">
<name>Update Installation Instructions</name>
<files>
  <modify>README.md</modify>
</files>
<action>
Add "Installation (Beta)" section to README.md detailing BRAT installation from the 'dist' branch.
</action>
<verify>
<automated>grep "BRAT" README.md</automated>
Expected: Section found.
</verify>
<done>README updated for beta users.</done>
</task>

<task type="auto">
<name>Final Quality & Tracking Audit</name>
<files>
  <modify>.gitignore</modify>
  <modify>README.md</modify>
</files>
<action>
Run full build and check git tracking one last time.
</action>
<verify>
<automated>npm run build &amp;&amp; git ls-files *.js *.css</automated>
Expected: Build succeeds, no root artifacts tracked.
</verify>
<done>Phase 18 audit complete.</done>
</task>
