---
phase: 18
plan: '01'
type: 'chore'
wave: 1
depends_on: ['17']
files_modified:
    [
        '.gitignore',
        'src/main.ts',
        'src/styles.css',
        '.config/esbuild.config.mjs',
        'SPEC.md',
        'CHANGELOG.md',
        'lint_output.txt',
        'tsc_out.txt',
        'lint_results.txt',
        'docs/SPEC.md',
        'docs/CHANGELOG.md',
    ]
autonomous: true
requirements: ['REQ-18.1', 'REQ-18.2']
must_haves:
    truths:
        - 'Root directory should be clean of build artifacts.'
    artifacts:
        - 'src/styles.css'
        - 'styles.css (generated)'
---

# 18-01: Root Cleanup & Build Refactor

**Goal:** Clean up the root directory and consolidate CSS generation.

<task type="auto">
<name>Relocate Documentation & AI Planning Files</name>
<files>
  <delete>SPEC.md</delete>
  <delete>CHANGELOG.md</delete>
  <create>docs/SPEC.md</create>
  <create>docs/CHANGELOG.md</create>
  <delete>lint_output.txt</delete>
  <delete>tsc_out.txt</delete>
  <delete>lint_results.txt</delete>
</files>
<action>
1. Move SPEC.md and CHANGELOG.md to docs/ directory.
2. Verify .planning/docs/ contains CLAUDE.md, REVIEW.md, QUICK-TASKS.md.
3. Remove temporary .txt files from root: lint_output.txt, tsc_out.txt, lint_results.txt.
4. Stage relocated docs for tracking: `git add docs/`
</action>
<verify>
<automated>ls docs/SPEC.md docs/CHANGELOG.md</automated>
Expected: Files exist in docs/. Root is clean of specified .txt files.
</verify>
<done>Files moved and root decluttered.</done>
</task>

<task type="auto">
<name>Harden .gitignore & Untrack Artifacts</name>
<files>
  <modify>.gitignore</modify>
  <modify>main.js</modify>
  <modify>worker.js</modify>
  <modify>ladybug-worker.js</modify>
  <modify>styles.css</modify>
  <modify>main.css</modify>
</files>
<action>
1. Update .gitignore to include:
main.js
worker.js
ladybug-worker.js
styles.css
main.css

2. Run: `git ls-files main.js worker.js ladybug-worker.js styles.css main.css | % { git rm --cached $_ }`
   </action>
   <verify>
   <automated>git ls-files _.js _.css</automated>
   Expected: No root artifacts tracked.
   </verify>
   <done>Git tracking hardened.</done>
   </task>

<task type="auto">
<name>Consolidate CSS & Automate Output</name>
<files>
  <modify>src/main.ts</modify>
  <modify>.config/esbuild.config.mjs</modify>
  <modify>src/styles.css</modify>
</files>
<action>
1. Import './styles.css' in src/main.ts.
2. In .config/esbuild.config.mjs, add a post-build hook to rename 'main.css' to 'styles.css'.
</action>
<verify>
<automated>npm run build</automated>
Expected: `styles.css` generated in root containing all styles.
</verify>
<done>CSS consolidated and build automated.</done>
</task>
