---
phase: '11'
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
    - src/types.ts
    - src/core/SuggestionExecutor.ts
autonomous: true
requirements:
    - UX-02
must_haves:
    truths:
        - 'When relinking A->B->C, all files update or none do (atomicity).'
        - 'Original frontmatter state is captured before modifications are made.'
        - 'User can reverse complex executions via an undo method.'
    artifacts:
        - path: src/types.ts
          provides: 'Extended HistoryItem with mementoData'
          min_lines: 5
        - path: src/core/SuggestionExecutor.ts
          provides: 'Atomic execution and undo functionality'
          min_lines: 50
    key_links:
        - from: src/core/SuggestionExecutor.ts
          to: src/types.ts
          via: 'HistoryItem.mementoData usage'
---

<objective>
Implement strong atomicity and memento-based reversibility for complex topological repairs in the SuggestionExecutor.

Purpose: To ensure that multi-file repairs (like A->B->C chains) do not leave the graph in a corrupted state if one file fails to write, and to allow the user to easily reverse complex actions per user decision D-01 and D-02.
Output: Hardened `SuggestionExecutor.ts` with rollback logic, Memento capture, and an `undo` method.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types.ts
@src/core/SuggestionExecutor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend HistoryItem with Memento Data</name>
  <files>src/types.ts</files>
  <action>
    Modify the `HistoryItem` interface to support the Memento Pattern (Decision 2).
    Add an optional property `mementoData` which is an array of objects describing the previous state of the modified files.
    The structure should be:
    ```typescript
    mementoData?: Array<{
        path: string;
        property: string;
        originalValue: unknown;
    }>;
    ```
    This allows storing exactly what the value was before the SuggestionExecutor modified it.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>HistoryItem interface successfully extended.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Atomicity and Memento Capture in Relink</name>
  <files>src/core/SuggestionExecutor.ts</files>
  <action>
    Update `innerExecuteRelink` to implement Strong Atomicity (Decision 1) and Memento Pattern (Decision 2).
    1. Before calling `processFrontMatter` on file A, B, and C, fetch their current frontmatter values for the properties that will be modified (using `app.metadataCache.getFileCache(file)?.frontmatter` or similar safe reads).
    2. Store these original values in a `mementoData` array.
    3. Wrap the three `processFrontMatter` calls in a try-catch block.
    4. If any `processFrontMatter` fails, manually revert the files that succeeded by calling `processFrontMatter` again using the values stored in `mementoData`, then `return false`.
    5. Update `finalizeSuggestion` signature to accept `mementoData` and pass it when pushing the `HistoryItem` to `this.context.cache.pushHistory`.
    6. Ensure the `executeRelink` success path calls `finalizeSuggestion` with the generated `mementoData`.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>innerExecuteRelink correctly captures original state, handles rollback on failure, and persists memento in history.</done>        
</task>

<task type="auto">
  <name>Task 3: Implement Undo Functionality</name>
  <files>src/core/SuggestionExecutor.ts</files>
  <action>
    Add a new method `async undo(historyItem: HistoryItem): Promise<boolean>` to `SuggestionExecutor`.
    This method should:
    1. Check if `historyItem.mementoData` exists. If not, return false.
    2. Iterate over the `mementoData` array.
    3. For each entry, resolve the `TFile` via `app.vault.getAbstractFileByPath`.
    4. Use `app.fileManager.processFrontMatter` to restore the `originalValue` to the specified `property`.
    5. If all restorations succeed, return `true`.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>undo method is available for reversing complex executions.</done>
</task>

<task type="auto">
  <name>Task 4: Council Workflow (Test & Review)</name>
  <files>src/core/SuggestionExecutor.ts, src/types.ts</files>
  <action>
    Follow the mandatory Council Workflow from GEMINI.md:
    1. Internal Test: Run `npm run test` to ensure baseline stability. Fix any issues.
    2. External Review: Run the `/council:review` command to get feedback from Kilo AI.
    3. Fix & Harden: Apply high/medium priority fixes from the review.
    4. Final Verification: Re-run `npm run test` and summarize the result.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Internal tests pass, external review completed, and core fixes applied.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                                   |
| --------------------- | ------------------------------------------------------------------------------------------------------------- |
| File System -> Plugin | Untrusted frontmatter values restored during rollback or undo could be malformed if manipulated concurrently. |

## STRIDE Threat Register

| Threat ID | Category        | Component                 | Disposition | Mitigation Plan                                                                                                                 |
| --------- | --------------- | ------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-11-01   | Tampering       | SuggestionExecutor (Undo) | accept      | Accept risk of user manually changing frontmatter between execution and undo, as Obsidian vaults are local and user-controlled. |
| T-11-02   | Info Disclosure | HistoryItem (Memento)     | accept      | Accept risk of frontmatter values stored in PluginContext memory/cache. Local plugin environment.                               |

</threat_model>

<verification>
- `HistoryItem` has `mementoData`.
- `executeRelink` properly captures the original state of all 3 files before mutating.
- Rollback logic triggers if a file write fails midway.
- `undo` method is present.
- Internal Test and `/council:review` steps have been performed.
</verification>

<success_criteria>

- The backend engine can safely and atomically perform a multi-file relink operation.
- State is preserved for reversibility.
- Code passed external review per council workflow.
  </success_criteria>

<output>
After completion, create `.planning/phases/11-complex-suggestion-execution/11-01-SUMMARY.md`
</output>
