---
phase: '11'
plan: '02'
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
    - src/views/components/ConfirmationModal.ts
    - src/views/dashboard/DashboardStore.svelte.ts
    - src/views/dashboard/components/Dashboard.svelte
autonomous: true
requirements:
    - UX-02
must_haves:
    truths:
        - 'User sees a pre-flight confirmation modal before complex executions.'
        - 'User can undo a complex execution directly from the UI.'
    artifacts:
        - path: src/views/components/ConfirmationModal.ts
          provides: 'Obsidian Modal for pre-flight confirmation'
          min_lines: 30
        - path: src/views/dashboard/DashboardStore.svelte.ts
          provides: 'Confirmation integration and undo triggering'
          min_lines: 20
    key_links:
        - from: src/views/dashboard/DashboardStore.svelte.ts
          to: src/core/SuggestionExecutor.ts
          via: 'executor.undo() call'
---

<objective>
Implement the UI feedback and reversibility flows for complex suggestion executions.

Purpose: To fulfill the user decision D-03 (Pre-flight Confirmation Modal) before executing multi-file edits, and provide the user access to the Undo functionality built in the previous plan.
Output: An Obsidian Modal class for confirmation, wired into the Svelte dashboard, with an actionable Undo mechanism.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/SuggestionExecutor.ts
@src/views/dashboard/DashboardStore.svelte.ts
@src/views/dashboard/components/Dashboard.svelte

<interfaces>
From src/types.ts:
```typescript
export interface SuggestionMeta {
    sourcePath?: string;
    targetPath?: string;
    winner?: string;
    property?: string;
    // ...
}
export interface HistoryItem {
    timestamp: number;
    action: string;
    file: string;
    type: string;
    mementoData?: Array<{ path: string; property: string; originalValue: unknown }>;
}
```
From src/core/SuggestionExecutor.ts:
```typescript
export class SuggestionExecutor {
    async executeRelink(suggestion: Suggestion): Promise<boolean>;
    async undo(historyItem: HistoryItem): Promise<boolean>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pre-flight Confirmation Modal</name>
  <files>src/views/components/ConfirmationModal.ts</files>
  <action>
    Create an Obsidian `Modal` subclass named `ConfirmationModal`.
    It should accept an `App` instance, a `Suggestion` representing the complex fix (e.g., bridge gap), and a callback function `onConfirm: () => void`.
    In `onOpen()`, render a breakdown of the 3 files that will be touched (derive from `suggestion.meta` properties like `sourcePath`, `targetPath`, `winner`).
    Include a clear message explaining that the edit spans multiple files.
    Render a "Confirm Execution" button (triggers callback and closes) and a "Cancel" button (closes).
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>ConfirmationModal visually outlines the multi-file changes and guards execution.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Modal into DashboardStore</name>
  <files>src/views/dashboard/DashboardStore.svelte.ts</files>
  <action>
    Add a method `async executeComplex(suggestion: Suggestion)` to `DashboardStore`.
    Inside the method, instantiate and open the `ConfirmationModal`.
    Only if confirmed via the modal's callback, await `this.#plugin.executor.executeRelink(suggestion)`.
    If execution succeeds, add the suggestion ID to `#fixedItems` and `refresh()` to update UI state.
    Create a new method `async undoAction(historyItem: HistoryItem)` that calls `await this.#plugin.executor.undo(historyItem)` and refreshes the state.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>executeComplex method opens modal and waits for confirmation before execution.</done>
</task>

<task type="auto">
  <name>Task 3: Expose Actions in Dashboard UI</name>
  <files>src/views/dashboard/components/Dashboard.svelte</files>
  <action>
    Update the template to handle complex suggestions (e.g., those requiring `executeRelink`, typically `type === 'topology_gap'` or identified by a specific ID prefix).
    Ensure the execution button for these complex suggestions triggers the new `executeComplex(suggestion)` method instead of the standard simple execution flow.
    Add UI representation for the Undo functionality: if a `historyItem` in the dashboard has `mementoData`, render an "Undo" button next to it. Clicking it calls `store.undoAction(historyItem)`.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Users can trigger complex executions (seeing the modal) and undo them from the history list.</done>
</task>

<task type="auto">
  <name>Task 4: Council Workflow (Test & Review)</name>
  <files>src/views/dashboard/components/Dashboard.svelte</files>
  <action>
    Follow the mandatory Council Workflow from GEMINI.md:
    1. Internal Test: Run `npm run test` to ensure baseline stability. Fix any issues.
    2. External Review: Run the `/council:review` command to get feedback from Kilo AI.
    3. Fix & Harden: Apply high/medium priority fixes from the review.
    4. Final Verification: Re-run `npm run test` and summarize the result.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Internal tests pass, external review completed, and fixes applied.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                                             |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------- |
| UI -> Plugin Core | User confirmation triggers multi-file writes. Untrusted values from UI input or suggestion meta are passed to executor. |

## STRIDE Threat Register

| Threat ID | Category    | Component             | Disposition | Mitigation Plan                                                                                               |
| --------- | ----------- | --------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- |
| T-11-03   | Spoofing    | ConfirmationModal     | accept      | Accept that the user could spoof local UI state. The executor handles path validation via Obsidian Vault API. |
| T-11-04   | Repudiation | DashboardStore (Undo) | accept      | Plugin history logs provide a local audit trail for what was undone, though not cryptographically signed.     |

</threat_model>

<verification>
- `ConfirmationModal.ts` is created and functional.
- `DashboardStore.svelte.ts` uses `ConfirmationModal` before executing complex relinks.
- `Dashboard.svelte` correctly exposes "Undo" buttons for reversible history items.
- Internal Test and `/council:review` steps have been performed.
</verification>

<success_criteria>

- The user is transparently informed about multi-file topological repairs before they happen.
- The user can seamlessly undo these complex repairs via the dashboard UI.
- All code has been tested and reviewed by Kilo AI.
  </success_criteria>

<output>
After completion, create `.planning/phases/11-complex-suggestion-execution/11-02-SUMMARY.md`
</output>
