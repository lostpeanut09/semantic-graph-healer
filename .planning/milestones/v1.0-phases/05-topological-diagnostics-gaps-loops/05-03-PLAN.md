---
phase: 05-topological-diagnostics-gaps-loops
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [src/core/TopologyAnalyzer.ts, src/types.ts, src/views/sections/ExperimentalSettings.ts]
autonomous: true
requirements: [TOPOL-01, TOPOL-04, TOPOL-05]
user_setup: []
must_haves:
    truths:
        - 'Bridge Scrutiny uses worker results with Depth 2 (D-01)'
        - 'Ouroboros detection respects Universal vs Boundary setting (D-02)'
        - 'Black Hole detection uses In-degree >= 7 threshold (D-03)'
        - 'Diagnostics are triggered automatically during graph scans (D-04)'
    artifacts:
        - path: 'src/core/TopologyAnalyzer.ts'
          provides: 'Refactored diagnostic methods using worker data'
        - path: 'src/types.ts'
          provides: 'Settings for Ouroboros scope'
    key_links:
        - from: 'TopologyAnalyzer'
          to: 'GraphEngine.runTopologicalAnalysis'
          via: 'async call in runDeterministicAnalysis or dedicated method'
---

<objective>
Refactor `TopologyAnalyzer` to utilize the worker-delegated topological diagnostics and implement the specific policy decisions (Depth=2, Cycle scope setting, In-degree >= 7). This completes the integration and ensures the features are user-accessible and high-performance.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/core/TopologyAnalyzer.ts
@src/types.ts
@.planning/phases/05-topological-diagnostics-gaps-loops/05-02-SUMMARY.md
@.planning/phases/05-topological-diagnostics-gaps-loops/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update settings and types</name>
  <files>src/types.ts, src/views/sections/ExperimentalSettings.ts</files>
  <action>
    1. Add `ouroborosScope: 'universal' | 'boundary'` to `SemanticGraphHealerSettings` in `src/types.ts`. Default to 'universal' (Per D-02).
    2. Add `blackHoleThreshold: number` to settings (default 7, Per D-03).
    3. Update `ExperimentalSettings.ts` (or relevant settings section) to include a toggle for Ouroboros Scope and a numeric input for Black Hole Threshold.
  </action>
  <verify>
    <automated>grep "ouroborosScope" src/types.ts</automated>
  </verify>
  <done>Settings updated to support user customization of topological diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor TopologyAnalyzer</name>
  <files>src/core/TopologyAnalyzer.ts</files>
  <behavior>
    - runBridgeScrutiny: Should call GraphEngine and map raw results to Suggestions.
    - runCycleAnalysis: Should filter cycles if 'boundary' scope is selected (cross-folder/cross-hierarchy).
    - runFlowStagnationAnalysis: Should use worker-detected sinks with threshold >= 7.
  </behavior>
  <action>
    1. Modify `TopologyAnalyzer.runBridgeScrutiny`:
       - Call `graphEngine.runTopologicalAnalysis({ bridgeDepth: 2 })`.
       - Map returned `bridges` to `Suggestion` objects with `type: 'topology_gap'`.
    2. Modify `TopologyAnalyzer.runCycleAnalysis`:
       - Call `graphEngine.runTopologicalAnalysis()`.
       - Filter results if `settings.ouroborosScope === 'boundary'` by checking if cycle nodes span multiple folders or hierarchies.
       - Map to `Suggestion` objects with `type: 'quality'`.
    3. Modify `TopologyAnalyzer.runFlowStagnationAnalysis`:
       - Use `graphEngine.runTopologicalAnalysis({ blackHoleThreshold: settings.blackHoleThreshold })`.
       - Map `blackHoles` to `Suggestion` objects with `category: 'suggestion'`, source: "Flow Stagnation: High in-degree, zero out-degree." (Per D-03).
    4. Ensure these are called as part of the automatic trigger (D-04).
  </action>
  <verify>
    <automated>npm test tests/core/TopologyAnalyzer.test.ts</automated>
  </verify>
  <done>TopologyAnalyzer is refactored for worker-delegated, high-performance diagnostics.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                      |
| ------------- | -------------------------------- |
| UI → Settings | User configuration of thresholds |

## STRIDE Threat Register

| Threat ID  | Category  | Component | Disposition | Mitigation Plan                                                                          |
| ---------- | --------- | --------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-05-03-01 | Tampering | Settings  | mitigate    | Validate numeric thresholds to be within sane ranges (1-50) using Zod or UI constraints. |

</threat_model>

<success_criteria>
Suggestions for bridges, cycles, and black holes appear correctly based on worker data, respecting user-defined thresholds and scopes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-topological-diagnostics-gaps-loops/05-03-SUMMARY.md`
</output>
