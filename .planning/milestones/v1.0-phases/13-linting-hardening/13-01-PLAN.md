---
phase: 13-linting-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.config/eslint.config.js, .husky/pre-commit, .husky/pre-push]
autonomous: true
requirements: [HARDEN-04, HARDEN-06]

must_haves:
    truths:
        - 'ESLint recognizes Svelte 5 runes ($state, $derived, etc.) as valid globals.'
        - 'Husky hooks are configured to run lint:fix + format on commit and build + test + knip on push.'
        - 'Scripts directory is correctly scoped for Node.js environment without browser globals.'
    artifacts:
        - path: '.config/eslint.config.js'
          provides: 'Svelte 5 rune globals and scripts/**/*.ts Node.js scoped override'
        - path: '.husky/pre-commit'
          provides: 'Quality gate: lint:fix + format before every local commit'
        - path: '.husky/pre-push'
          provides: 'Quality gate: build + test + knip before every remote push'
        - path: 'scripts/generate-mock-vault.ts'
          provides: 'Node.js build script exercises the scoped scripts override'
---

<objective>
Establish the technical foundation for repository hardening by updating the ESLint configuration to fully recognise Svelte 5 runes and to scope the `scripts/` directory for Node.js built-ins, and by upgrading the Husky hooks so that commits and pushes are gated on lint, format, build, test, and knip.

**Purpose:** Prevent regression and ensure all local and CI contributors follow the same strict standards before code leaves their machine.

**Output:**

- `.config/eslint.config.js` — Svelte 5 runes confirmed in `languageOptions.globals`; `scripts/**/*.ts` override verified.
- `.husky/pre-commit` — Runs `npm run lint:fix` then `npm run format`; exits non-zero on any failure.
- `.husky/pre-push` — Runs `npm run build`, `npm test`, and `npx knip`; exits non-zero on any failure.

---

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@.config/eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ESLint Config for Svelte 5 and Scoped Scripts</name>
  <files>.config/eslint.config.js</files>
  <action>
    - **Step 1 – Verify Svelte 5 runes** (already present, confirm they are listed in the `['**/*.ts']` block `languageOptions.globals`):
      - `$state: 'readonly'`
      - `$derived: 'readonly'`
      - `$effect: 'readonly'`
      - `$props: 'readonly'`
      - `$inspect: 'readonly'`
      - `$host: 'readonly'`
      - `$bindable: 'readonly'`
    - **Step 2 – Verify `svelte.ts` store coverage** — The `['**/*.ts']` glob already covers `.svelte.ts` store files. No glob change needed.
    - **Step 3 – Verify `scripts/**/*.ts` override** — Confirm the block at lines 103–127 has:
      - `no-console: 'off'`
      - `import/no-nodejs-modules: 'off'`
      - Node-only globals (`process`, `__dirname`, `__filename`, `require`, `module`)
      - Explicitly disabled browser globals (`window: 'off'`, `document: 'off'`, `navigator: 'off'`, `fetch: 'off'`)
    - **Step 4 – Document `kilo_review.mjs`** — The file is matched by the top-level `ignores: ['**/*.mjs']` rule (line 131). Add an inline ESLint config comment in the config file explaining that `.mjs` reviewer scripts are intentionally ignored rather than silently excluded from the scripts override.
  </action>
  <verify>
    <automated>npm run lint src/views/dashboard/DashboardStore.svelte.ts scripts/generate-mock-vault.ts</automated>
  </verify>
  <done>
    Svelte 5 runes are recognised without ESLint errors; `scripts/generate-mock-vault.ts` passes with only Node.js-valid globals active; no `import/no-nodejs-modules` or browser-global errors appear in the lint output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Husky Hooks for CI/CD Alignment</name>
  <files>.husky/pre-commit, .husky/pre-push</files>
  <action>
    - **Step 1 – Update `.husky/pre-commit`:**
      - Keep `npm run lint:fix` with exit-code check (already present).
      - Keep `npm run format` with exit-code check (already present).
      - Remove any slow-phase checks (`build`, `test`) from pre-commit — this stays fast.
      - Verify the shebang (`#!/usr/bin/env sh`) is present (it is implicitly correct for the heredoc/echo style).
    - **Step 2 – Update `.husky/pre-push`:**
      - Keep `npm run build` with exit-code check (already present).
      - Keep `npm test` with exit-code check (already present).
      - Add `npx knip` (Dead code analysis) to align with CLAUDE.md Quality Assurance Pipeline.
      - Consider whether `npm run lint` (non-fix, warn-level check) should be appended via `|| exit 1` to catch any remaining non-auto-fixable warnings before the remote push — document the decision in STATE.md.
    - **Step 3 – Wire into `package.json`** — Confirm `"prepare": "husky"` exists so that `npm install` / `npm ci` always installs the hooks in new environments.
  </action>
  <verify>
    <automated>ls .husky/pre-commit .husky/pre-push && grep "npx knip" .husky/pre-push</automated>
  </verify>
  <done>
    Both hook files exist, are executable, and `pre-push` contains `npm run build` and `npx knip`.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                                                          |
| ------------- | ------------------------------------------------------------------------------------ |
| Git -> Remote | Husky hooks act as the final local filter before code reaches the shared repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan  
 |
| ---------- | -------- | ------------ | ----------- | -------------------------------------------------------------------------------------------- |
| T-13-01-01 | Tampering | `.husky` hooks | mitigate | Ensure hook files are tracked in git and bootstrapped via `prepare: "husky"` in `package.json`. Verify hooks are executable (permissions preserved) so developers cannot silently bypass them with `git commit --no-verify` without audit trail. |

</threat_model>

<verification>
Run `npm run lint src/views/dashboard/DashboardStore.svelte.ts scripts/generate-mock-vault.ts` — exit code must be 0 and no Svelte 5 or Node-global errors should be reported.

Run `ls .husky/pre-commit .husky/pre-push && grep "npx knip" .husky/pre-push` — both files must exist and `pre-push` must contain the build and knip step.
</verification>

<success_criteria>

- ESLint executes without errors for Svelte 5 rune references and Node.js globals in `scripts/`.
- `.husky/pre-commit` exists, is executable, and runs `lint:fix` + `format`.
- `.husky/pre-push` exists, is executable, and runs `build` + `test` + `knip`.
- `package.json` contains `"prepare": "husky"`.

</success_criteria>

<output>
After completion, create `.planning/phases/13-linting-hardening/13-01-SUMMARY.md`
</output>
