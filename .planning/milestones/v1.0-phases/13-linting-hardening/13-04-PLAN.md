---
phase: 13-linting-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
    - package.json
    - src/types.ts
autonomous: true
gap_closure: true
requirements: [HARDEN-05, HARDEN-06]
must_haves:
    truths:
        - 'Run `npm run build` exits with code 0 and emits no TypeScript errors'
        - 'TypeScript union intersection error on MultiGraph in src/types.ts is resolved'
    artifacts:
        - path: package.json
          provides: 'Build dependencies'
        - path: src/types.ts
          provides: 'Core type definitions'
    key_links: []
---

<objective>
Resolve TypeScript build errors and MultiGraph intersection issues.

Purpose: Fix critical type resolution errors and missing dev dependencies found during UAT.
Output: Working build and clean type definitions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-linting-hardening/13-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MultiGraph Type Error</name>
  <files>src/types.ts</files>
  <action>
    Fix the MultiGraph type intersection/union to resolve the TS error.
    Gap reason: TypeScript union intersection error on MultiGraph.
    Review how MultiGraph is exported from graphology types or simplify the definition to resolve intersection issues.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>MultiGraph type resolves without intersection errors.</done>
</task>

<task type="auto">
  <name>Task 2: Install missing @types dependencies</name>
  <files>package.json</files>
  <action>
    Install `@types/node` and `@types/graphology` as dev dependencies using `npm install -D @types/node @types/graphology`.
    Gap reason: Build fails with TS errors missing @types/node and @types/graphology.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build completes successfully with no TS errors related to node or graphology.</done>
</task>

</tasks>

<verification>
npm run build exits with 0 and shows no TS errors.
</verification>

<success_criteria>
Build passes and types resolve cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/13-linting-hardening/13-04-SUMMARY.md`
</output>
