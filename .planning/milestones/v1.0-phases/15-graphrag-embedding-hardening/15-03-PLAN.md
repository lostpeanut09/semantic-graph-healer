---
phase: 15-graphrag-embedding-hardening
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
    [
        src/core/services/GraphRagService.ts,
        src/core/utils/AjsonStorage.ts,
        src/core/services/EntityExtractor.ts,
        src/main.ts,
        tests/core/utils/AjsonStorage.test.ts,
        tests/core/services/GraphRagService.index.test.ts,
        tests/core/services/EntityExtractor.test.ts,
        tests/core/services/GraphRagService.query.test.ts,
    ]
autonomous: true
requirements: [AI-05, AI-06]
must_haves:
    truths:
        - 'Plugin generates summaries and vector embeddings for Louvain-detected communities.'
        - 'Background worker extracts entities and relationships into AJSON storage (AI-06).'
        - 'Summaries and entity tables are stored in .planning/index/ as AJSON files.'
        - 'Users can perform semantic searches that leverage community context and summary vectors.'
    artifacts:
        - path: 'src/core/services/GraphRagService.ts'
          provides: 'Community-centric summarization and RAG query orchestration'
        - path: 'src/core/utils/AjsonStorage.ts'
          provides: 'High-performance JSON-per-line storage'
        - path: 'src/core/services/EntityExtractor.ts'
          provides: 'Background entity and relationship indexing'
    key_links:
        - from: 'src/core/services/GraphRagService.ts'
          to: 'src/core/LlmService.ts'
          via: 'Community summarization calls'
---

<objective>
Build the GraphRAG indexing and query engine. Implement community-centric summarization with vector search, background entity extraction, and scalable AJSON-based storage.

Purpose: Enable deep semantic search and context-aware RAG within the Obsidian vault.
Output: GraphRagService, AjsonStorage utility, EntityExtractor, and populated .planning/index/ directory.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-graphrag-embedding-hardening/15-CONTEXT.md
@.planning/phases/15-graphrag-embedding-hardening/15-RESEARCH.md
@.planning/phases/15-graphrag-embedding-hardening/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AjsonStorage for Scalable Indexing</name>
  <files>src/core/utils/AjsonStorage.ts, tests/core/utils/AjsonStorage.test.ts</files>
  <action>
    Create `AjsonStorage` utility to handle JSON-per-line (AJSON) persistence (per D-02).
    - Implement `appendLine(filePath: string, data: object): Promise<void>`.
    - Implement `readAll(filePath: string): Promise<object[]>`.
    - Implement `upsert(filePath: string, key: string, data: object): Promise<void>`.
    - Use this for storing entity tables and community reports in `.planning/index/`.
  </action>
  <verify>
    <automated>npm test tests/core/utils/AjsonStorage.test.ts</automated>
  </verify>
  <done>AjsonStorage correctly handles concurrent writes and large-file reads in Obsidian.</done>
</task>

<task type="auto">
  <name>Task 2: Build Community Summarization & Vector Pipeline</name>
  <files>src/core/services/GraphRagService.ts, tests/core/services/GraphRagService.index.test.ts</files>
  <action>
    Implement the community-centric summarization and vector indexing pipeline (per AI-05, D-02).
    - For each Louvain cluster detected by the `GraphEngine`, generate a summary using `LlmService`.
    - Prompt should include: "Summarize the shared theme of these notes: [List of titles]".
    - **Crucial:** Generate a vector embedding for each summary using `EmbeddingService`.
    - Persist summaries and their vectors in `.planning/index/community_summaries.ajson`.
    - Implement a background indexing task that runs when the vault is idle.
  </action>
  <verify>
    <automated>npm test tests/core/services/GraphRagService.index.test.ts</automated>
  </verify>
  <done>Community summaries and vectors are generated and persisted for all major clusters.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Background Entity & Relationship Extraction</name>
  <files>src/core/services/EntityExtractor.ts, tests/core/services/EntityExtractor.test.ts</files>
  <action>
    Implement background extraction of entities and relationships (per AI-06, D-02).
    - Create `EntityExtractor` to parse notes for People, Projects, and Concepts.
    - Extract relationships between entities based on co-occurrence or explicit linking.
    - Store extracted data in `.planning/index/entities.ajson` and `.planning/index/relationships.ajson` using `AjsonStorage`.
    - Use debounced triggers to extract data as notes are modified or during idle time.
  </action>
  <verify>
    <automated>npm test tests/core/services/EntityExtractor.test.ts</automated>
  </verify>
  <done>Entity and relationship index is maintained in the background via AJSON storage.</done>
</task>

<task type="auto">
  <name>Task 4: Implement GraphRAG Query Logic</name>
  <files>src/core/services/GraphRagService.ts, tests/core/services/GraphRagService.query.test.ts</files>
  <action>
    Build the retrieval logic for semantic queries (per AI-05).
    - Step 1: Search community summaries for relevant clusters using **vector similarity** against the summary embeddings.
    - Step 2: Retrieve relevant notes from selected clusters and check the entity index for related context.
    - Step 3: Combine community context + entity facts + note content into a final RAG prompt.
    - Step 4: Call `LlmService` for the final answer.
  </action>
  <verify>
    <automated>npm test tests/core/services/GraphRagService.query.test.ts</automated>
  </verify>
  <done>GraphRAG query engine provides contextually rich answers based on community and entity indices.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                           |
| --------------------- | ------------------------------------- |
| File System -> Memory | Loading large AJSON files into memory |

## STRIDE Threat Register

| Threat ID | Category            | Component    | Disposition | Mitigation Plan                                                |
| --------- | ------------------- | ------------ | ----------- | -------------------------------------------------------------- |
| T-15-05   | Resource Exhaustion | AjsonStorage | mitigate    | Use line-by-line streams for reading large indices             |
| T-15-06   | Tampering           | Index Files  | accept      | User-local files, risk of corruption handled by reload/rebuild |

</threat_model>

<verification>
1. `npm test tests/core/utils/AjsonStorage.test.ts` passes.
2. `npm test tests/core/services/GraphRagService.index.test.ts` passes.
3. `npm test tests/core/services/EntityExtractor.test.ts` passes.
4. Manual verification: Trigger "Generate Index", confirm `.planning/index/` files exist.
</verification>

<success_criteria>

- Community reports and entity indices provide a high-level semantic map.
- AJSON storage prevents memory issues on large vaults.
- GraphRAG queries are significantly more context-aware than simple vector search.
  </success_criteria>

<output>
Create `.planning/phases/15-graphrag-embedding-hardening/15-03-SUMMARY.md` when done
</output>
