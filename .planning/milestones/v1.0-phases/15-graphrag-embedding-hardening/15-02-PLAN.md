---
phase: 15-graphrag-embedding-hardening
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
    [
        src/core/LlmService.ts,
        src/core/GraphEngine.ts,
        src/core/workers/graph-analysis-core.ts,
        src/core/TopologyAnalyzer.ts,
        tests/core/LlmService.prefilter.test.ts,
        tests/core/workers/GraphAnalysisCore.htrv2.test.ts,
        tests/core/TopologyAnalyzer.incongruence.test.ts,
    ]
autonomous: true
requirements: [HARDEN-08, HARDEN-09]
must_haves:
    truths:
        - 'AI Tribunal fast-fails suggestions with semantic similarity below 0.4.'
        - 'HTR v2 incorporates cosine similarity into edge weighting.'
        - "New 'semantic_incongruence' diagnostic flags links where content has diverged from topology."
    artifacts:
        - path: 'src/core/LlmService.ts'
          provides: 'Stage 0 semantic pre-filtering in AI Tribunal'
        - path: 'src/core/workers/graph-analysis-core.ts'
          provides: 'Vector-weighted centrality (HTR v2)'
    key_links:
        - from: 'src/core/LlmService.ts'
          to: 'src/core/EmbeddingService.ts'
          via: 'getEmbedding for pre-filter'
---

<objective>
Integrate embeddings into the core decision-making and ranking algorithms. Implement AI Tribunal pre-filtering to reduce LLM costs and HTR v2 for semantically-aware graph centrality.

Purpose: Increase precision and efficiency of graph suggestions.
Output: Hardened AI Tribunal and HTR v2 ranking.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-graphrag-embedding-hardening/15-CONTEXT.md
@.planning/phases/15-graphrag-embedding-hardening/15-RESEARCH.md
@.planning/phases/15-graphrag-embedding-hardening/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AI Tribunal Stage 0 Pre-filtering</name>
  <files>src/core/LlmService.ts, tests/core/LlmService.prefilter.test.ts</files>
  <action>
    Add a "Stage 0" pre-filter to the AI Tribunal logic in `LlmService.ts` (per HARDEN-08, D-03).
    - Before calling any LLM for verification, calculate the cosine similarity between the source and target note embeddings.
    - If `cosineSimilarity < 0.4`, fast-fail the suggestion with status `REJECTED` and reason `SEMANTIC_UNRELATED`.
    - Log this as a "Tribunal Bypassed (Stage 0)" event.
    - Ensure this reduces LLM calls by at least 20% in standard benchmarks.
  </action>
  <verify>
    <automated>npm test tests/core/LlmService.prefilter.test.ts</automated>
  </verify>
  <done>AI Tribunal correctly fast-fails semantically unrelated suggestions without calling the LLM.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HTR v2 Vector-Weighted Centrality</name>
  <files>src/core/workers/graph-analysis-core.ts, src/core/GraphEngine.ts, tests/core/workers/GraphAnalysisCore.htrv2.test.ts</files>
  <action>
    Upgrade the Healer Topological Rank (HTR) to version 2 (per HARDEN-09, D-04).
    - In the worker's graph analysis logic, incorporate vector similarity into edge weights.
    - Formula: `Edge Weight = (StructuralWeight * htrStructuralWeight) + (VectorSimilarity * (1 - htrStructuralWeight))`.
    - Pass `vectorEmbeddings` from the main thread to the worker during full graph rebuild.
    - Update PageRank and Louvain community detection to use these weighted edges.
  </action>
  <verify>
    <automated>npm test tests/core/workers/GraphAnalysisCore.htrv2.test.ts</automated>
  </verify>
  <done>HTR v2 scores correctly reflect both topological structure and semantic similarity.</done>
</task>

<task type="auto">
  <name>Task 3: Add Semantic Incongruence Diagnostic</name>
  <files>src/core/TopologyAnalyzer.ts, tests/core/TopologyAnalyzer.incongruence.test.ts</files>
  <action>
    Implement a new diagnostic type: `semantic_incongruence` (per D-04).
    - Scan existing links in the graph.
    - If a link exists but the cosine similarity between the two notes is below a threshold (e.g., 0.2), flag it as a "Semantic Incongruence".
    - Suggestion: "Review link - notes appear to have diverged semantically."
  </action>
  <verify>
    <automated>npm test tests/core/TopologyAnalyzer.incongruence.test.ts</automated>
  </verify>
  <done>Plugin successfully detects and flags existing links that are semantically weak.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                              |
| --------------------- | ---------------------------------------- |
| Main Thread -> Worker | Data transfer of large embedding records |

## STRIDE Threat Register

| Threat ID | Category          | Component   | Disposition | Mitigation Plan                                                                |
| --------- | ----------------- | ----------- | ----------- | ------------------------------------------------------------------------------ |
| T-15-03   | Denial of Service | GraphWorker | mitigate    | Implement chunked transfer for embeddings if memory pressure exceeds threshold |
| T-15-04   | Spoofing          | HTR Scores  | accept      | Low risk, internal ranking metric                                              |

</threat_model>

<verification>
1. `npm test tests/core/LlmService.prefilter.test.ts` passes.
2. `npm test tests/core/workers/GraphAnalysisCore.htrv2.test.ts` passes.
3. Manual verification: Check "Tribunal Audit" in dashboard for "Bypassed (Stage 0)" status.
</verification>

<success_criteria>

- Tribunal pre-filtering reduces LLM costs.
- HTR v2 provides more relevant community clusters and rankings.
- Semantic incongruence helps maintain graph hygiene over time.
  </success_criteria>

<output>
Create `.planning/phases/15-graphrag-embedding-hardening/15-02-SUMMARY.md` when done
</output>
