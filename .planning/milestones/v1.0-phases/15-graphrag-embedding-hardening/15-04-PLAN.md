---
phase: 15-graphrag-embedding-hardening
plan: 04
type: execute
wave: 4
depends_on: [15-03]
files_modified:
    [
        src/views/GraphRagView.svelte,
        src/views/sections/AiSettings.ts,
        src/views/SettingsTab.ts,
        src/core/EmbeddingService.ts,
        src/core/CrossThematicProvider.ts,
        tests/views/GraphRagView.test.ts,
        tests/views/Settings.semantic.test.ts,
        tests/core/CrossThematicProvider.test.ts,
    ]
autonomous: false
requirements: [UX-09, HARDEN-08]
must_haves:
    truths:
        - 'Users can access a dedicated GraphRAG tab in the Healer Dashboard.'
        - 'Deep Search toggle is available in the AI view for RAG-enhanced reasoning.'
        - "The plugin generates 'Cross-Thematic Bridge' suggestions between semantically related but topologically distant communities (SC#5)."
        - 'Settings show real-time embedding model health (STABLE/MISALIGNED).'
    artifacts:
        - path: 'src/views/GraphRagView.svelte'
          provides: 'Global knowledge query interface'
        - path: 'src/core/CrossThematicProvider.ts'
          provides: 'Bridge suggestions between distant clusters using GraphRAG index'
    key_links:
        - from: 'src/views/GraphRagView.svelte'
          to: 'src/core/services/GraphRagService.ts'
          via: 'Query execution'
---

<objective>
Expose GraphRAG and embedding hardening features in the UI. Implement Cross-Thematic suggestion logic and provide a dedicated dashboard tab for global queries.

Purpose: Make advanced semantic capabilities accessible and bridge knowledge gaps across the vault.
Output: GraphRagView.svelte, CrossThematicProvider, and updated Settings UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-graphrag-embedding-hardening/15-CONTEXT.md
@.planning/phases/15-graphrag-embedding-hardening/15-RESEARCH.md
@.planning/phases/15-graphrag-embedding-hardening/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GraphRAG Dashboard Tab</name>
  <files>src/views/GraphRagView.svelte, src/views/SettingsTab.ts, tests/views/GraphRagView.test.ts</files>
  <action>
    Implement the GraphRAG dashboard tab (per UX-09, D-05).
    - Create `GraphRagView.svelte` with a search bar and a results area.
    - Wire it to `GraphRagService.query()`.
    - Display "Community Context" used for each query (which clusters were pulled).
    - Add the tab to the main `SettingsTab.ts` / Dashboard view.
  </action>
  <verify>
    <automated>npm test tests/views/GraphRagView.test.ts</automated>
  </verify>
  <done>Users can perform GraphRAG queries from the dashboard and see the results.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Deep Search Toggle and Model Health UI</name>
  <files>src/views/sections/AiSettings.ts, src/views/sections/TribunalSettings.ts, tests/views/Settings.semantic.test.ts</files>
  <action>
    Update AI and Tribunal settings to reflect new semantic capabilities (per UX-09, HARDEN-08, D-03).
    - Add a "Deep Search" toggle to the AI settings. When enabled, suggestions use `GraphRagService` for reasoning context.
    - Add a "Model Health" status indicator to the Embedding settings (pulling from `EmbeddingService.modelStatus`).
    - Display a warning if the model is "MISALIGNED" during the Semantic Anchor check.
  </action>
  <verify>
    <automated>npm test tests/views/Settings.semantic.test.ts</automated>
  </verify>
  <done>Settings UI correctly exposes Deep Search control and reflects embedding model quality.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Cross-Thematic Suggestion Provider</name>
  <files>src/core/CrossThematicProvider.ts, tests/core/CrossThematicProvider.test.ts</files>
  <action>
    Implement `CrossThematicProvider` to generate bridge suggestions between distant communities (per Success Criterion #5).
    - Use the community summary embeddings from the GraphRAG index to find clusters that are semantically similar but have few or no topological links between them.
    - Identify representative nodes (highest HTR v2 score) in each cluster.
    - Propose "Cross-Thematic Bridge" links between these nodes.
    - Integrate with the `SuggestionEngine` to surfacing these suggestions in the dashboard.
  </action>
  <verify>
    <automated>npm test tests/core/CrossThematicProvider.test.ts</automated>
  </verify>
  <done>Plugin generates bridge suggestions between semantically related but topologically distant communities.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full GraphRAG, Cross-Thematic Suggestions, and Embedding Hardening UI integration.</what-built>
  <how-to-verify>
    1. Open Semantic Graph Healer Settings.
    2. Go to "Embedding" section, verify "Model Health" is STABLE.
    3. Open Dashboard, click "GraphRAG" tab.
    4. Type a global query and verify thematic results.
    5. Check "Suggestions" view for "Cross-Thematic Bridge" links.
  </how-to-verify>
  <resume-signal>Type "approved" if UI is intuitive and "Bridge" links make semantic sense.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                   |
| ---------- | --------------------------------------------- |
| UI -> User | Displaying AI reasoning and community context |

## STRIDE Threat Register

| Threat ID | Category               | Component      | Disposition | Mitigation Plan                                     |
| --------- | ---------------------- | -------------- | ----------- | --------------------------------------------------- |
| T-15-07   | Information Disclosure | Result Display | mitigate    | Sanitize all RAG outputs before rendering in Svelte |
| T-15-08   | UX Denial of Service   | Query Hang     | mitigate    | Add loading states and timeout for RAG queries      |

</threat_model>

<verification>
1. `npm test tests/views/GraphRagView.test.ts` passes.
2. `npm test tests/core/CrossThematicProvider.test.ts` passes.
3. Manual UAT: Query flow, bridge suggestion validation.
</verification>

<success_criteria>

- Users can query their entire vault using GraphRAG.
- Cross-thematic suggestions bridge topologically distant but semantically related clusters.
- Model misalignment is clearly communicated to prevent poor quality suggestions.
  </success_criteria>

<output>
Create `.planning/phases/15-graphrag-embedding-hardening/15-04-SUMMARY.md` when done
</output>
