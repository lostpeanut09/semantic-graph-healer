---
phase: 15-graphrag-embedding-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    [
        src/core/EmbeddingService.ts,
        src/core/CacheService.ts,
        src/types.ts,
        src/types.schema.ts,
        tests/core/EmbeddingService.test.ts,
        tests/core/CacheService.test.ts,
        tests/core/EmbeddingService.alignment.test.ts,
    ]
autonomous: true
requirements: [INFRA-08, HARDEN-08]
must_haves:
    truths:
        - 'User can configure local embedding providers (Ollama/LocalAI) in settings.'
        - 'Plugin generates embeddings for notes using the selected local model.'
        - 'Embeddings are cached and persisted across sessions.'
        - 'Semantic Anchor check fails if the model returns garbage/misaligned vectors.'
    artifacts:
        - path: 'src/core/EmbeddingService.ts'
          provides: 'Native embedding generation with local-first priority'
        - path: 'src/core/CacheService.ts'
          provides: 'Vector embedding persistence'
    key_links:
        - from: 'src/core/EmbeddingService.ts'
          to: 'Ollama/LocalAI API'
          via: 'requestUrl'
---

<objective>
Establish the foundational embedding infrastructure, including the native EmbeddingService, vector caching, and the Semantic Anchor hardening check.

Purpose: Enable deep semantic indexing while ensuring model quality and local-first privacy.
Output: EmbeddingService.ts, updated CacheService.ts, and embedding configuration in Settings.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-graphrag-embedding-hardening/15-CONTEXT.md
@.planning/phases/15-graphrag-embedding-hardening/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement EmbeddingService with Local-First Support</name>
  <files>src/core/EmbeddingService.ts, src/types.ts, src/types.schema.ts, tests/core/EmbeddingService.test.ts</files>
  <action>
    Create `EmbeddingService` to handle vector generation via Ollama (`/api/embeddings`) and LocalAI/OpenAI (`/v1/embeddings`) (per D-01).
    - Use Obsidian's `requestUrl` for all network calls.
    - Implement `getEmbedding(text: string): Promise<number[]>` with retry logic.
    - Add settings for `embeddingProvider`, `embeddingModel`, `embeddingEndpoint`, and `embeddingDimensions` to `src/types.ts` and `src/types.schema.ts`.
    - Implement fallback to Smart Connections only if local providers are unavailable/disabled.
  </action>
  <verify>
    <automated>npm test tests/core/EmbeddingService.test.ts</automated>
  </verify>
  <done>EmbeddingService correctly fetches vectors from mock Ollama/LocalAI endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Extend CacheService for Vector Persistence</name>
  <files>src/core/CacheService.ts, tests/core/CacheService.test.ts</files>
  <action>
    Update `CacheService` to store and retrieve note embeddings (per INFRA-08).
    - Add `vectorEmbeddings` record to the cache schema: `Record<string, { vector: number[], hash: string }>`.
    - Implement `getStoredEmbedding(notePath: string, contentHash: string): number[] | null`.
    - Implement `storeEmbedding(notePath: string, vector: number[], contentHash: string): void`.
    - Ensure embeddings are persisted to `healer-cache.json`.
  </action>
  <verify>
    <automated>npm test tests/core/CacheService.test.ts</automated>
  </verify>
  <done>Embeddings are successfully saved to and loaded from healer-cache.json.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Semantic Anchors Hardening</name>
  <files>src/core/EmbeddingService.ts, tests/core/EmbeddingService.alignment.test.ts</files>
  <action>
    Implement `checkModelAlignment(): Promise<boolean>` (per HARDEN-08, D-03).
    - Hardcode 10 concept pairs (e.g., "king/queen", "apple/fruit") with expected similarity bounds.
    - Run this check during `EmbeddingService` initialization or on-demand.
    - If similarity falls outside expected ranges (e.g., "king" and "queen" are 0.1 similar), flag the model as "misaligned/stupid".      
    - Expose `modelStatus` (STABLE | MISALIGNED | OFFLINE) for UI consumption.
  </action>
  <verify>
    <automated>npm test tests/core/EmbeddingService.alignment.test.ts</automated>
  </verify>
  <done>Semantic Anchor check correctly identifies low-quality or misaligned embedding models.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                            |
| ------------------- | -------------------------------------- |
| Plugin -> Local API | Untrusted response from Ollama/LocalAI |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                                                                       |
| --------- | ---------------------- | ---------------- | ----------- | ------------------------------------------------------------------------------------- |
| T-15-01   | Tampering              | EmbeddingService | mitigate    | Validate vector dimensions and value ranges (non-NaN)                                 |
| T-15-02   | Information Disclosure | RequestUrl       | mitigate    | Ensure API keys (if used for LocalAI/OpenAI fallback) are handled via KeychainService |
| T-15-SC   | Tampering              | npm installs     | mitigate    | slopcheck + blocking human checkpoint for [ASSUMED] packages                          |

</threat_model>

<verification>
1. `npm test tests/core/EmbeddingService.test.ts` passes.
2. `npm test tests/core/CacheService.test.ts` passes.
3. Manual verification: Configure Ollama in settings, run "Check Model", confirm "STABLE" status.
</verification>

<success_criteria>

- EmbeddingService retrieves vectors from local endpoints.
- Vectors are cached by content hash to avoid redundant API calls.
- Semantic Anchor check prevents usage of broken/low-quality models.
  </success_criteria>

<output>
Create `.planning/phases/15-graphrag-embedding-hardening/15-01-SUMMARY.md` when done
</output>
