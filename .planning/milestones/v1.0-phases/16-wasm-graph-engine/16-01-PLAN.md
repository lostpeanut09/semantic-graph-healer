---
phase: 16-wasm-graph-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    [
        package.json,
        .config/esbuild.config.mjs,
        src/core/workers/ladybug-worker.ts,
        src/core/services/LadybugService.ts,
        src/core/adapters/LadybugAdapter.ts,
        tests/benchmarks/LadybugBenchmark.test.ts,
        src/core/adapters/LadybugAdapter.test.ts,
        src/core/services/LadybugService.test.ts,
    ]
autonomous: false
requirements: [INFRA-06, INFRA-07, HARDEN-10, PERF-01]

must_haves:
    truths:
        - 'LadybugDB initializes in a background worker without blocking the main UI thread.'
        - 'Tiered fallback correctly selects best available engine (MT-WASM > ST-WASM > Legacy).'
        - 'Graph schema (Node/SemanticLink) is versioned and successfully applied.'
        - 'Topological diagnostics return correct results via Cypher queries.'
        - 'Benchmarks on 50k nodes demonstrate >10x speedup within 256MB RAM.'
    artifacts:
        - path: 'src/core/workers/ladybug-worker.ts'
          provides: 'LadybugDB WASM worker entry point with tiered threading support'
          min_lines: 60
        - path: 'src/core/services/LadybugService.ts'
          provides: 'Service for managing LadybugDB lifecycle, states, and tiered fallback'
          exports: ['LadybugService']
        - path: 'src/core/adapters/LadybugAdapter.ts'
          provides: 'Data sync bridge with schema versioning checks'
          exports: ['LadybugAdapter']
    key_links:
        - from: 'src/core/adapters/LadybugAdapter.ts'
          to: 'LadybugDB'
          via: 'Batched Cypher INSERT/COPY'
        - from: 'src/core/services/LadybugService.ts'
          to: 'LadybugWorker'
          via: 'Web Worker message bridge'
---

<objective>
Implement the LadybugDB WASM graph engine to support large-scale vaults (50k+ nodes). This involves a rigorous package audit for `icebug`, setting up a tiered fallback infrastructure (handling SharedArrayBuffer restrictions), defining a versioned graph schema, and porting topological diagnostics to Cypher with performance verification.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-16/RESEARCH.md
@.planning/phases/phase-16/16-CONTEXT.md
@.planning/phases/phase-16/16-VALIDATION.md
@src/core/workers/graph-analysis-core.ts
@src/core/GraphEngine.ts
@src/core/adapters/IMetadataAdapter.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Rigorous Package Audit & Pivot Decision (icebug)</name>
  <files>STATE.md, RESEARCH.md</files>
  <action>
    1. Search NPM and GitHub for the `icebug` package.
    2. Confirm if it is a legitimate high-performance NetworKit fork for LadybugDB as claimed in RESEARCH.md.
    3. If `icebug` cannot be verified or found:
       - **Pivot Recommendation**: Switch to a direct WASM port of NetworKit or a mature alternative like `igraph` for the algorithm layer.
       - Document the decision in STATE.md and update RESEARCH.md accordingly.
  </action>
  <verify>
    Human confirmation of package legitimacy or pivot selection.
    1. Visit https://www.npmjs.com/package/icebug
    2. Confirm legitimate source/origin.
  </verify>
  <done>A verified graph algorithm package is selected and recorded.</done>
  <resume-signal>Selected package: [name]</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Tiered Infrastructure & Worker Bridge</name>
  <files>package.json, .config/esbuild.config.mjs, src/core/workers/ladybug-worker.ts, src/core/services/LadybugService.ts, src/core/services/LadybugService.test.ts</files>
  <behavior>
    - Test 1: LadybugService exposes initialization states: 'none' -> 'loading' -> 'ready'.
    - Test 2: Tiered Fallback: If SharedArrayBuffer is missing, it successfully initializes Single-Threaded LadybugDB. If WASM fails entirely, it falls back to Legacy.
    - Test 3: LadybugWorker fetches the 12MB binary only on first query, and sends an "init-progress" message during loading.
  </behavior>
  <action>
    1. Install `@ladybugdb/wasm-core` (v0.16.1) and the verified algorithm package from Task 0.
    2. Add `ladybug-worker: 'src/core/workers/ladybug-worker.ts'` as a new entry point in `.config/esbuild.config.mjs` (target browser, format iife).
    3. Create `src/core/workers/ladybug-worker.ts`. Implement logic to detect `SharedArrayBuffer` support and initialize LadybugDB in either Multi-Threaded or Single-Threaded mode.
    4. Create `src/core/services/LadybugService.ts`. It must:
       - Manage the worker lifecycle.
       - Track and expose `initializationStatus` to the UI.
       - Implement the tiered fallback logic (MT-WASM -> ST-WASM -> Legacy).
    5. Ensure the worker sends progress updates during WASM compilation to support an "Initializing Engine..." UI state.
  </action>
  <verify>
    <automated>npm run build && npm test src/core/services/LadybugService.test.ts</automated>
  </verify>
  <done>LadybugDB worker is bundled with tiered threading support, and the service manages the lifecycle with explicit states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Versioned Schema Definition & Data Sync</name>
  <files>src/core/workers/ladybug-worker.ts, src/core/adapters/LadybugAdapter.ts, src/core/adapters/LadybugAdapter.test.ts</files>
  <behavior>
    - Test 1: LadybugAdapter performs a schema version check on startup; drops and recreates tables if version mismatch detected.
    - Test 2: Node and Relation tables are created with correct schemas: Node(path, label, size), SemanticLink(FROM Node TO Node, weight, type).
    - Test 3: Syncing 1000 nodes completes without errors and data is queryable via Cypher.
  </behavior>
  <action>
    1. Define a `Metadata` table in LadybugDB to store the `schema_version`.
    2. Implement schema evolution logic in the worker: check `schema_version`; if outdated or missing, drop existing tables and recreate.
    3. Implement `LadybugAdapter.ts`: subscribe to `IMetadataAdapter` events and sync changes to LadybugDB using batched Cypher statements.
    4. Optimize ingestion for large vaults using LadybugDB's `COPY` command or optimized batching.
  </action>
  <verify>
    <automated>npm test src/core/adapters/LadybugAdapter.test.ts</automated>
  </verify>
  <done>Versioned graph schema is applied and vault data is synchronized into LadybugDB with migration support.</done>
</task>

<task type="auto">
  <name>Task 3: Cypher Algos & Memory-Aware Benchmarking</name>
  <files>src/core/workers/ladybug-worker.ts, tests/benchmarks/LadybugBenchmark.test.ts</files>
  <action>
    1. Port topological diagnostics to Cypher:
       - **Black Holes**: `MATCH (n) WHERE outdegree(n) = 0 AND indegree(n) > $threshold RETURN n.path`
       - **Bridges**: `MATCH (a)-[r1]->(b)-[r2]->(c) WHERE r1.type = r2.type AND NOT (a)-[]->(c) AND a <> c RETURN b.path`
       - **Cycles**: Use LadybugDB's `RECURSIVE` path search.
    2. Integrate algorithms (PageRank, Louvain) from the verified package.
    3. Implement `tests/benchmarks/LadybugBenchmark.test.ts`:
       - Generate synthetic graphs of 50,000 nodes.
       - Measure query execution time and verify >10x speedup vs legacy.
       - **Memory Monitoring**: Log `performance.memory.usedJSHeapSize` (if available) or use a memory limit check to ensure usage stays under 256MB.
  </action>
  <verify>
    <automated>npx vitest tests/benchmarks/LadybugBenchmark.test.ts</automated>
  </verify>
  <done>Cypher diagnostics are fully functional and performance/memory targets are verified for large vaults.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                     |
| --------------------- | ------------------------------- |
| Main Thread -> Worker | Structured cloning of link data |
| Worker -> LadybugDB   | WASM memory boundary (isolated) |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                                                                                                       |
| --------- | ---------------------- | ---------------- | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-16-01   | DoS                    | LadybugDB Worker | mitigate    | Implement execution timeouts for Cypher queries (default 60s) to prevent UI lock if a complex query hangs the worker. |
| T-16-02   | Information Disclosure | IndexedDB        | mitigate    | WASM persistence uses standard browser isolation; no cross-vault or cross-plugin data leakage is possible.            |
| T-16-SC   | Tampering              | npm installs     | mitigate    | Rigorous audit of `icebug` or pivot to verified library in Task 0; blocking human checkpoint.                         |

</threat_model>

<verification>
1. `npm run build` confirms `ladybug-worker.js` is correctly bundled.
2. `npm test src/core/services/LadybugService.test.ts` confirms tiered fallback (MT/ST/Legacy).
3. `npm test src/core/adapters/LadybugAdapter.test.ts` passes with schema versioning.
4. `npx vitest tests/benchmarks/LadybugBenchmark.test.ts` confirms >10x speedup and <256MB memory.
</verification>

<success_criteria>

- LadybugDB initializes successfully in background worker with proper tiered fallback.
- Initialization progress is reported to the UI during lazy-loading of 12MB WASM.
- Cypher queries return identical results to legacy Graphology implementation.
- PageRank and Louvain benchmarks show >10x speedup on 50k+ nodes.
- Memory usage stays within 256MB for 50k node vault.
- Schema versioning handles potential future evolution without data loss.
  </success_criteria>

<output>
Create `.planning/phases/phase-16/16-01-SUMMARY.md` when done
</output>
