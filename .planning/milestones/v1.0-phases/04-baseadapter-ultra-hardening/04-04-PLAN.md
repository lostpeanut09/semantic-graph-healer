---
phase: 04-baseadapter-ultra-hardening
plan: 04
type: execute
wave: 2
depends_on: ['04-01']
files_modified: [src/core/adapters/DatacoreAdapter.ts, src/core/adapters/BreadcrumbsAdapter.ts]
autonomous: true
requirements: [HARDEN-03d]
must_haves:
    truths:
        - 'DatacoreAdapter and BreadcrumbsAdapter implement the initialization protocol'
        - 'Calling getLinks before initialize throws an error'
    artifacts:
        - path: 'src/core/adapters/DatacoreAdapter.ts'
          provides: 'Hardened Datacore integration'
        - path: 'src/core/adapters/BreadcrumbsAdapter.ts'
          provides: 'Hardened Breadcrumbs integration'
---

<objective>
Apply the initialization guard pattern to DatacoreAdapter and BreadcrumbsAdapter to ensure architectural consistency and prevent race conditions across all metadata sources.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-baseadapter-ultra-hardening/04-01-SUMMARY.md
@src/core/adapters/DatacoreAdapter.ts
@src/core/adapters/BreadcrumbsAdapter.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden DatacoreAdapter with Init Guards</name>
  <files>src/core/adapters/DatacoreAdapter.ts</files>
  <behavior>
    - Implement `onInitialize()` to handle any Datacore-specific setup.
    - Call `this.ensureInitialized()` in all public data retrieval methods.
  </behavior>
  <action>
    Refactor DatacoreAdapter.ts:
    1. Implement `protected async onInitialize(): Promise<void>`.
    2. Wrap existing initialization logic (if any) into `onInitialize`.
    3. Add `this.ensureInitialized()` guards to `getLinks()`, `getPage()`, etc.
  </action>
  <verify>
    <automated>npm test tests/core/adapters/DatacoreAdapter.test.ts</automated>
  </verify>
  <done>DatacoreAdapter is hardened against premature calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden BreadcrumbsAdapter with Init Guards</name>
  <files>src/core/adapters/BreadcrumbsAdapter.ts</files>
  <behavior>
    - Implement `onInitialize()` to handle Breadcrumbs API verification.
    - Call `this.ensureInitialized()` in all public data retrieval methods.
  </behavior>
  <action>
    Refactor BreadcrumbsAdapter.ts:
    1. Implement `protected async onInitialize(): Promise<void>`.
    2. Add `this.ensureInitialized()` guards to `getLinks()`, `getHierarchy()`, etc.
  </action>
  <verify>
    <automated>npm test tests/core/adapters/BreadcrumbsAdapter.test.ts</automated>
  </verify>
  <done>BreadcrumbsAdapter is hardened against premature calls.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                     |
| ---------------------- | ------------------------------------------------------------------------------- |
| External API → Adapter | Adapters must be fully initialized before interacting with external plugin APIs |

## STRIDE Threat Register

| Threat ID | Category  | Component           | Disposition | Mitigation Plan                                                                                            |
| --------- | --------- | ------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-04-04   | Tampering | Supporting Adapters | mitigate    | Use initialization guards to prevent inconsistent state if external APIs are called before they are ready. |

</threat_model>

<success_criteria>

- Both adapters pass initialization tests.
- Error is thrown if called before `initialize()` completes.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-baseadapter-ultra-hardening/04-04-SUMMARY.md`
</output>
