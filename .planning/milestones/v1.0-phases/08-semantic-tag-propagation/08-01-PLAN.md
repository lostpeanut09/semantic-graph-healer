---
phase: 08-semantic-tag-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/SemanticTagPropagator.ts, src/main.ts, docs/PLAN.md]
autonomous: true
requirements: [AI-02]
must_haves:
    truths:
        - 'SemanticTagPropagator correctly calculates majority tags for child clusters.'
        - 'Tag propagation suggestions are generated when coverage exceeds the threshold.'
        - 'Exclusion list (MOC, Index, Dashboard) is respected.'
    artifacts:
        - path: 'src/core/SemanticTagPropagator.ts'
          provides: 'Core logic for hierarchical tag propagation'
        - path: 'src/main.ts'
          provides: 'Integration of tag propagation into the global analysis scan'
---

<objective>
Implement and Integrate Semantic Tag Propagation.

Purpose: To automate the discovery and suggestion of missing tags in hierarchical clusters based on parent semantics.
Output: Fully integrated SemanticTagPropagator.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-semantic-tag-propagation/08-CONTEXT.md
@.planning/phases/08-semantic-tag-propagation/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Initialize Phase 8 Plan</name>
  <files>docs/PLAN.md</files>
  <action>Create or update docs/PLAN.md with Phase 8 scope, acceptance criteria, and test commands.</action>
  <verify>
    <automated>grep -c "Phase 8" docs/PLAN.md</automated>
  </verify>
  <done>docs/PLAN.md initialized.</done>
</task>

<task type="auto">
  <name>Task 1: Refine SemanticTagPropagator</name>
  <files>src/core/SemanticTagPropagator.ts</files>
  <action>Refine the existing implementation to respect the `tagPropagationExclusions` list. Ensure coverage calculation correctly handles nested tags and normalization. Add proper logging for propagation events.</action>
  <verify>
    <automated>grep -c "tagPropagationExclusions" src/core/SemanticTagPropagator.ts</automated>
  </verify>
  <done>Propagator refined with exclusion logic and improved coverage math.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into Main Analysis</name>
  <files>src/main.ts</files>
  <action>Verify that `tagPropagator.runTagPropagationAnalysis()` is called during the global `analyzeGraph()` scan and that results are merged into the suggestion cache. (Note: Initial scan of main.ts shows it might already be partially integrated, confirm and complete).</action>
  <verify>
    <automated>grep -c "this.tagPropagator.runTagPropagationAnalysis()" src/main.ts</automated>
  </verify>
  <done>Tag propagation is a first-class citizen in the global graph analysis loop.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Unit Tests</name>
  <files>tests/core/SemanticTagPropagator.test.ts</files>
  <action>Create a new test file to verify tag propagation logic: majority detection, threshold respect, and exclusion list filtering.</action>
  <verify>
    <automated>npx vitest run tests/core/SemanticTagPropagator.test.ts</automated>
  </verify>
  <done>Logic verified via automated unit tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                      |
| ---------------------------- | -------------------------------- |
| Vault Metadata -> Propagator | Reading tags and hierarchy links |

## STRIDE Threat Register

| Threat ID | Category               | Component             | Disposition | Mitigation Plan                                                                                                            |
| --------- | ---------------------- | --------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------- |
| T-08-01   | Information Disclosure | SemanticTagPropagator | mitigate    | Ensure tag propagation logic doesn't leak private tags if a 'private' prefix is detected (out of scope for now but noted). |

</threat_model>

<verification>
Run `npm run build` and `npx vitest run tests/core/SemanticTagPropagator.test.ts`.
</verification>

<success_criteria>
Tag propagation suggestions appear in the dashboard when parent clusters share common tags, respecting user-defined thresholds and exclusions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-semantic-tag-propagation/08-01-SUMMARY.md`
</output>
