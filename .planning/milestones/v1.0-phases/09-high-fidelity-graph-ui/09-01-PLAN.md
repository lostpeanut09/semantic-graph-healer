---
phase: 09-high-fidelity-graph-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/core/utils/GraphMapper.ts, tests/core/utils/GraphMapper.test.ts]
autonomous: true
requirements: [UI-01]
must_haves:
    truths:
        - 'Graphology data can be converted to force-graph compatible {nodes, links} format'
        - 'Hierarchical cycles and structural gaps are correctly identified in the mapping'
    artifacts:
        - path: 'src/core/utils/GraphMapper.ts'
          provides: 'Graphology to Force-Graph data bridge'
        - path: 'tests/core/utils/GraphMapper.test.ts'
          provides: 'Unit tests for mapping logic'
    key_links:
        - from: 'src/core/utils/GraphMapper.ts'
          to: 'src/core/GraphEngine.ts'
          via: 'Graphology data ingestion'
          pattern: 'mapToForceGraph(engine.getGraph())'
---

<objective>
Install 3D graph dependencies and implement the data mapping bridge between the internal Graphology engine and the force-graph rendering format.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-high-fidelity-graph-ui/09-RESEARCH.md
@src/types.ts
@src/core/GraphEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Install Graph dependencies</name>
  <files>package.json</files>
  <action>
    Install the required libraries for 3D graph rendering:
    `npm install 3d-force-graph d3-force-3d`
    Also install `three` as it is a peer dependency of `3d-force-graph`.
  </action>
  <verify>
    <automated>npm list 3d-force-graph d3-force-3d three</automated>
  </verify>
  <done>Dependencies installed and listed in package.json</done>
</task>

<task type="auto" tdd="true">
  <name>Implement GraphMapper utility</name>
  <files>src/core/utils/GraphMapper.ts, tests/core/utils/GraphMapper.test.ts</files>
  <behavior>
    - Should map Graphology nodes to ForceGraph nodes with `id` and labels.
    - Should map Graphology edges to ForceGraph links with `source` and `target`.
    - Should detect Ouroboros (cycle) attribute and set `isCycle: true`.
    - Should detect Structural Gaps (bridge_gap) and set `isGhost: true`.
  </behavior>
  <action>
    Create `src/core/utils/GraphMapper.ts` providing a `mapToForceGraph` function.
    Use the patterns identified in RESEARCH.md for data conversion.
    Ensure `graphology` MultiGraph attributes are correctly extracted.
  </action>
  <verify>
    <automated>npx vitest tests/core/utils/GraphMapper.test.ts</automated>
  </verify>
  <done>GraphMapper utility implemented and verified with tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                         |
| ------------------------- | --------------------------------------------------- |
| Graphology -> GraphMapper | Trusted internal data is transformed for rendering. |

## STRIDE Threat Register

| Threat ID  | Category               | Component   | Disposition | Mitigation Plan                                                                        |
| ---------- | ---------------------- | ----------- | ----------- | -------------------------------------------------------------------------------------- |
| T-09-01-01 | Information Disclosure | GraphMapper | accept      | Graph data is already loaded in memory; mapper only transforms it for local rendering. |

</threat_model>

<success_criteria>

1. 3d-force-graph and d3-force-3d are installed.
2. GraphMapper correctly handles cycles and ghost edges.
3. Unit tests pass for various graph structures.
   </success_criteria>

<output>
After completion, create `.planning/phases/09-high-fidelity-graph-ui/09-01-SUMMARY.md`
</output>
