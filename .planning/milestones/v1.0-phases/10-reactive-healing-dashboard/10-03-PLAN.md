---
phase: 10-reactive-healing-dashboard
plan: 03
type: execute
wave: 3
depends_on: ['10-02']
files_modified:
    [
        src/views/dashboard/DashboardStore.svelte.ts,
        src/views/dashboard/components/SuggestionCard.svelte,
        tests/views/dashboard/DashboardStore.test.ts,
    ]
autonomous: true
requirements: [UX-01]
must_haves:
    truths:
        - 'Batch fix operations do not freeze the UI'
        - 'Ignored items can be undone via toast'
        - 'Fixed items show visual feedback instead of disappearing'
    artifacts:
        - path: 'src/views/dashboard/DashboardStore.svelte.ts'
          provides: 'fixAll yielding loop and Undo logic'
    key_links:
        - from: 'DashboardStore.fixAll'
          to: 'executor.execute'
          via: 'sequential yielding loop'
---

<objective>
Implement bulk operations and harden the interaction model with yielding loops and undo capabilities for a responsive user experience.

Purpose: Provide a professional, responsive user experience for large-scale healing and prevent accidental data loss.
Output: Batch healing, interaction hardening features, and updated UI feedback.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reactive-healing-dashboard/10-02-SUMMARY.md
@src/views/dashboard/DashboardStore.svelte.ts
@src/views/dashboard/components/SuggestionCard.svelte
@src/core/SuggestionExecutor.ts
@src/views/DashboardView.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Batch Fix All logic</name>
  <files>src/views/dashboard/DashboardStore.svelte.ts</files>
  <action>
    Implement `fixAll(suggestions: Suggestion[])` in `DashboardStore.svelte.ts`.

    Requirements:
    - Use a `Notice` to show progress (e.g., "Fixing 5/20...").
    - Iterate through suggestions sequentially.
    - Call `this.plugin.executor.execute(s)`.
    - **Yielding:** Call `await new Promise(r => setTimeout(r, 0))` every 5 items to prevent UI lockup.
    - Handle errors gracefully and continue with the next item.
    - Update store state after each successful execution (mark as fixed).

  </action>
  <verify>
    <automated>grep "setTimeout" src/views/dashboard/DashboardStore.svelte.ts</automated>
  </verify>
  <done>Batch fix logic with yielding loop implemented.</done>
</task>

<task type="auto">
  <name>Implement Undo Toast & Ignore logic</name>
  <files>src/views/dashboard/DashboardStore.svelte.ts</files>
  <action>
    Implement `ignore(suggestion: Suggestion)` in `DashboardStore`.

    Requirements:
    1. Instantly remove the suggestion from the reactive state.
    2. Show an Obsidian `Notice` with a "Undo" button (using `DocumentFragment`).
    3. If "Undo" is clicked, restore the suggestion to the state and hide the notice.
    4. If notice expires, persist the ignore state in `plugin.settings.proximityIgnoreList`.

  </action>
  <verify>
    <automated>grep "DocumentFragment" src/views/dashboard/DashboardStore.svelte.ts</automated>
  </verify>
  <done>Ignore with Undo toast implemented in the store.</done>
</task>

<task type="auto">
  <name>Update SuggestionCard for Fixed State</name>
  <files>src/views/dashboard/components/SuggestionCard.svelte</files>
  <action>
    Update `SuggestionCard.svelte` to support visual feedback for execution.

    Requirements:
    - When a suggestion is successfully executed, visually mark the card as "Fixed" (e.g., opacity change, checkmark icon).
    - Disable Execute/Ignore buttons for fixed items.
    - Use Svelte runes to react to the 'fixed' status of the suggestion in the store.

  </action>
  <verify>
    <automated>grep "Fixed" src/views/dashboard/components/SuggestionCard.svelte</automated>
  </verify>
  <done>SuggestionCard updated with fixed state feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Harden store tests for batch and ignore logic</name>
  <files>tests/views/dashboard/DashboardStore.test.ts</files>
  <behavior>
    - Mock `executor.execute` to return success.
    - Verify `fixAll` processes all items and yields.
    - Verify `ignore` removes item and `undo` restores it.
  </behavior>
  <action>
    Extend Vitest unit tests in `DashboardStore.test.ts` to cover the new hardening features.
  </action>
  <verify>
    <automated>npx vitest tests/views/dashboard/DashboardStore.test.ts --run</automated>
  </verify>
  <done>Hardened store logic (batch/ignore) verified by automated tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                   |
| -------------------------- | --------------------------------------------- |
| User Action -> File System | Batch operations perform multiple file edits. |

## STRIDE Threat Register

| Threat ID | Category          | Component  | Disposition | Mitigation Plan                                    |
| --------- | ----------------- | ---------- | ----------- | -------------------------------------------------- |
| T-10-03   | Denial of Service | Batch Loop | mitigate    | Yielding loop ensures Obsidian remains responsive. |

</threat_model>

<verification>
Verify `fixAll` uses sequential execution to avoid file system race conditions. Run `npx vitest tests/views/dashboard/DashboardStore.test.ts` for regression check of batch/ignore logic.
</verification>

<success_criteria>

1. "Fix All" button works without freezing Obsidian.
2. "Ignore" can be undone.
3. Executed suggestions show a "Fixed" state.
4. Batch and ignore store tests pass.
   </success_criteria>

<output>
After completion, create `.planning/phases/10-reactive-healing-dashboard/10-03-SUMMARY.md`
</output>
