---
phase: 12-v1-finalization-stress-testing
plan: 03
type: execute
wave: 3
depends_on: ['12-02']
files_modified: [src/core/GraphEngine.ts, src/views/GraphVisualizerView.ts]
autonomous: true
requirements: [V1-STRESS-01, V1-UI-01]

must_haves:
    truths:
        - 'Graph analysis is throttled in Safety Mode to preserve UI responsiveness.'
        - 'Graph View uses Level of Detail (LOD) optimizations when Safety Mode is active.'
        - "Analysis worker doesn't crash or hang with 10k+ nodes."
    artifacts:
        - path: 'src/core/GraphEngine.ts'
          provides: 'Throttled graph analysis logic'
          min_lines: 150
        - path: 'src/views/GraphVisualizerView.ts'
          provides: 'Optimized WebGL rendering for large graphs'
          min_lines: 200
    key_links:
        - from: 'GraphEngine'
          to: 'PerformanceService'
          via: 'mode check before analysis'
        - from: 'GraphVisualizerView'
          to: 'PerformanceService'
          via: 'rendering parameter adjustment'
---

<objective>
Optimize the Graph Engine and Visualization layer to handle large-scale vaults by implementing adaptive throttling and Level of Detail (LOD) rendering.

Purpose: Maintain 60 FPS in Standard mode and > 20 FPS in Safety Mode.
Output: Optimized GraphEngine and GraphVisualizerView.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-v1-finalization-stress-testing/12-CONTEXT.md
@.planning/phases/12-v1-finalization-stress-testing/12-RESEARCH.md
@src/core/GraphEngine.ts
@src/views/GraphVisualizerView.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Adaptive Analysis Throttling</name>
  <files>src/core/GraphEngine.ts</files>
  <action>
    Modify `GraphEngine.ts` to respect the `PerformanceMode`:
    - If `Safety`: Increase debounce for background file analysis (e.g., from 5s to 30s).
    - Limit `maxNodes` and `maxEdges` guardrails more strictly in Safety Mode if not overridden.
    - Suspend heavy algorithms (like Louvain Community Detection) during background scans, only running them on manual trigger.
    - Use lower sampling rates for Betweenness Centrality in Safety Mode.
  </action>
  <verify>
    <automated>npx vitest run tests/core/GraphEngine.test.ts (add tests for mode-based throttling)</automated>
  </verify>
  <done>Analysis logic adapts to vault size and performance state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Graph View Level of Detail (LOD)</name>
  <files>src/views/GraphVisualizerView.ts</files>
  <action>
    Optimize the 3D Graph View for Safety Mode:
    - Node Rendering: Use `nodeResolution(1)` (points) instead of spheres.
    - Physics: Disable "Live Physics" (freeze simulation after 100 iterations or a fixed time).
    - Links: Use thin lines instead of cylinders/thick lines.
    - Interaction: Disable hover effects for non-critical nodes when N > 5,000.
    - Frustum Culling: Ensure efficient disposal of nodes outside the view.
  </action>
  <verify>
    <automated>grep "nodeResolution" src/views/GraphVisualizerView.ts</automated>
  </verify>
  <done>Graph View remains interactive and non-laggy with 10k nodes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                                                                                                                  |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| UI â†’ Worker | Large message payloads to worker should be avoided; use transfers or shared buffers if possible (OOB for v1, focus on payload minimization). |

## STRIDE Threat Register

| Threat ID  | Category            | Component               | Disposition | Mitigation Plan                                                        |
| ---------- | ------------------- | ----------------------- | ----------- | ---------------------------------------------------------------------- |
| T-12-03-01 | Resource Exhaustion | src/core/GraphEngine.ts | mitigate    | Implement strict timeout for Web Worker tasks (workerTimeout setting). |

</threat_model>

<verification>
Run the benchmark suite from Plan 1 on the 10k mock vault and verify that UI frame rate stays above 20 FPS during graph manipulation.     
</verification>

<success_criteria>

- Frame rate > 20 FPS on 10k node graph in Safety Mode.
- Analysis worker finishes 10k node PageRank in < 5s (on modern desktop).
- No "Browser out of memory" crashes during stress tests.
  </success_criteria>

<output>
After completion, create `.planning/phases/12-v1-finalization-stress-testing/12-03-SUMMARY.md`
</output>
