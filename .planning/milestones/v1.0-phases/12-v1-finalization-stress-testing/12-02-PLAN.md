---
phase: 12-v1-finalization-stress-testing
plan: 02
type: execute
wave: 2
depends_on: ['12-01']
files_modified: [src/core/services/PerformanceService.ts, src/main.ts, src/views/SettingsTab.ts, src/types.ts]
autonomous: true
requirements: [V1-ADAPTIVE-01]

must_haves:
    truths:
        - 'Plugin detects vault size at startup.'
        - 'Safety Mode is suggested or auto-enabled when thresholds are exceeded.'
        - 'Users can manually toggle Safety Mode and adjust thresholds in settings.'
    artifacts:
        - path: 'src/core/services/PerformanceService.ts'
          provides: 'Performance mode detection and management'
          min_lines: 60
        - path: 'src/main.ts'
          provides: 'Startup integration of PerformanceService'
          min_lines: 100
    key_links:
        - from: 'src/main.ts'
          to: 'PerformanceService'
          via: 'initialization call in onload'
---

<objective>
Implement the Adaptive Performance (Safety Mode) state machine and settings to allow the plugin to proactively manage resource consumption in large vaults.

Purpose: Prevent UI freezes and excessive CPU usage in vaults > 10,000 notes.
Output: PerformanceService, updated SettingsTab, and main.ts integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-v1-finalization-stress-testing/12-CONTEXT.md
@.planning/phases/12-v1-finalization-stress-testing/12-RESEARCH.md
@src/main.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Performance Settings and Types</name>
  <files>src/types.ts, src/types.schema.ts</files>
  <action>
    Add the following to `SemanticGraphHealerSettings` and `DEFAULT_SETTINGS`:
    - `enableSafetyMode`: boolean (default false)
    - `safetyModeThresholdDesktop`: number (default 10000)
    - `safetyModeThresholdMobile`: number (default 2500)
    - `performanceMode`: 'Standard' | 'Safety' (not persisted, derived at runtime)
    Update `src/types.schema.ts` with Zod validation rules.
  </action>
  <verify>
    <automated>grep "enableSafetyMode" src/types.ts && npx vitest run tests/core/Settings.test.ts</automated>
  </verify>
  <done>Settings and schemas updated for performance management.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PerformanceService</name>
  <files>src/core/services/PerformanceService.ts</files>
  <action>
    Create a singleton service that:
    - Detects if the current platform is mobile (`Platform.isMobile`).
    - Calculates current vault size (`app.vault.getMarkdownFiles().length`).
    - Determines if Safety Mode should be active based on settings and vault size.
    - Provides a `getPerformanceMode()` method.
    - Manages a temporary session override (if user dismisses notice for current session).
  </action>
  <verify>
    <automated>npx vitest run tests/core/services/PerformanceService.test.ts</automated>
  </verify>
  <done>PerformanceService accurately determines the appropriate mode.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Performance Detection into Startup</name>
  <files>src/main.ts, src/views/SettingsTab.ts</files>
  <action>
    In `src/main.ts`:
    - Initialize `PerformanceService` in `onload()`.
    - If Safety Mode is triggered for the first time, show a non-blocking Notice or a simple Modal explaining the benefits.
    In `src/views/SettingsTab.ts`:
    - Add a new "Performance & Safety" section.
    - Include toggles for Safety Mode and threshold inputs.
  </action>
  <verify>
    <automated>grep "PerformanceService" src/main.ts && grep "Performance & Safety" src/views/SettingsTab.ts</automated>
  </verify>
  <done>Safety Mode is active at startup and configurable in settings.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                        |
| ------------------ | ---------------------------------------------------------------------------------- |
| Settings â†’ Logic | Threshold values must be validated to prevent division by zero or negative limits. |

## STRIDE Threat Register

| Threat ID  | Category          | Component                               | Disposition | Mitigation Plan                                                                              |
| ---------- | ----------------- | --------------------------------------- | ----------- | -------------------------------------------------------------------------------------------- |
| T-12-02-01 | Denial of Service | src/core/services/PerformanceService.ts | mitigate    | Ensure vault size detection is O(1) or O(N) but called infrequently (cached during session). |

</threat_model>

<verification>
Verify that opening a 10k note vault triggers the Safety Mode notice and that the settings correctly reflect the current performance state.
</verification>

<success_criteria>

- Vault size detection completes in < 10ms.
- Safety Mode correctly activates based on platform-specific thresholds.
- Settings changes take effect immediately without plugin reload.
  </success_criteria>

<output>
After completion, create `.planning/phases/12-v1-finalization-stress-testing/12-02-SUMMARY.md`
</output>
